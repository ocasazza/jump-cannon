[package]
name = "graph-renderer"
version = "0.1.0"
edition = "2021"
description = "Static HTML/CSS/JS bundle for the graph viewer; embedded via include_dir."

[lib]
name = "graph_renderer"
path = "src/lib.rs"

[dependencies]
include_dir = "0.7"
