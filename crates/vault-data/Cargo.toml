[package]
name = "vault-data"
version = "1.0.0"
edition = "2021"
description = "Canonical graph domain types shared by importers, metrics, APIs, and renderers"

[dependencies]
serde      = { workspace = true }
serde_json = { workspace = true }
indexmap   = { workspace = true }
petgraph   = { workspace = true }
