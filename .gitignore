node_modules
*.log*
.nuxt
.nitro
.cache
.output
.env
dist
/target
**/target/

# Trunk-built WASM frontend output for graph-renderer.
# Rebuilt locally via `just wasm` (or `trunk build`).
crates/graph-renderer/assets/dist/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/

# direnv layer (local nix env)
.direnv/

# Claude scratch + assorted local tooling
.claude/
.gc/
