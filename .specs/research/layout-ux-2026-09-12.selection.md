# Selection — Layout panel UI/UX pruning (2026-09-12)

## Vote tally (ranked choice: 1st=3pts, 2nd=2pts, 3rd=1pt)

| Proposal | Judge 1 | Judge 2 | Judge 3 | Points |
|---|---|---|---|---|
| **C2 — Engine capability declarations** | 1st (3.85) | 1st (3.88) | 3rd (gate-capped 2.00, pre-cap 3.58) | **7** |
| **C3 — Per-graph-class forms** | 3rd (3.38) | 2nd (3.00) | 1st (3.00, only ungated) | **6** |
| **C1 — Regime-as-data registry** | hold (pre-gate 3.80) | 3rd (gate-capped, pre-cap 3.87) | 2nd (gate-capped, pre-cap 4.00 — highest of all 18) | **3** |
| B1 — Intent sliders | 2nd (3.62) | capped | capped | 2 |
| A4, A2, others | ≤ hold | — | — | ≤1 |

## Selected for expansion

1. **C2** (7 pts) — per-engine capability manifests; inapplicable controls never constructed. Judge conditions to address: CK-006 (shows absence, not per-parameter provenance text — merge C1's why-capsule), CK-005 concern (local manifests must derive from existing serde schemas, not new compiled tables).
2. **C3** (6 pts) — per-graph-class honest forms (Vault/Molecular/Remote/Static). Judge conditions: CK-010 pitfall (bespoke-per-class proliferation — bound the form count, define seventh-case behavior), hybrid-class merge behavior (partially typed graphs).
3. **C1** (3 pts; pre-cap 3.80–4.00, highest uncapped score of all 18) — regime-as-data YAML registry. Gate failure was a two-sentence coverage omission (remote engines / static solvers never named); all three judges flagged it as top-3 worthy with the condition that expansion explicitly cover remote engines and static solvers. Selected per judge consensus over mechanical gate application.

## Consensus observations (all three judges)

- C1 and C2 are composable articulations of one pattern ("regimes-as-data + capabilities-as-data"); expansion should treat convergence as likely and keep the artifacts mergeable.
- The CK-001 essential gate dominated: 15–16 of 18 proposals never named remote engines/static solvers. Expansion MUST cover all six use cases explicitly.
- B1 (intent sliders) is the strongest interaction-model element and a likely synthesis ingredient for whichever structure wins: dimensionless multipliers write-through into GpuForceOptions, no engine change.
- Codebase claims were grep-verified by judges (TYPED_FORCE_SUMMARY, Monaco surface, Timeline panel, cursor forces, the false banner at layout.rs:3606–3609).

## Process note

Explorer phase ran degraded: task-agent model quota (429) forced in-session proposal generation across the three leans (A: information architecture, B: interaction model, C: system/architecture). Pruning phase ran at full rigor with three independent judge agents applying the meta-judge specification mechanically, including codebase verification of proposal premises.
