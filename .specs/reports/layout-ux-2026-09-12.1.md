# Evaluation Report — Layout Panel UI/UX Expansions (2026-09-12, judge 1)

## Metadata

- **User prompt:** Design a UI/UX pattern for Jump Cannon's Layout panel fitting all six use cases, replacing the flat-physics-constants panel; Rust+Dioxus only, app.css + panel_kit::CSS, 340–480px dockable panel, Operate-mode doctrine, packages-not-crates, no engine changes for the core pattern.
- **Artifacts:** `solution.a.md` (C2 — engine capability manifests), `solution.b.md` (C3 — per-graph-class forms), `solution.c.md` (C1 — regime-as-data registry)
- **Specification source:** `.specs/research/layout-ux-2026-09-12.eval-spec.yaml` (applied mechanically, in full)
- **Ground truth:** `.specs/research/layout-ux-context.md`, `.specs/research/layout-ux-2026-09-12.selection.md`

## RANKING (final adjusted score)

| Rank | Document | Weighted (pre-cap) | Pitfalls | Gate | **Final** | Label |
|---|---|---|---|---|---|---|
| **1** | **solution.a.md (C2 — capability manifests)** | 3.88 | 0 | CK-207 NO → cap | **2.00** | **select** |
| **2** | **solution.c.md (C1 — regime-as-data registry)** | 3.68 | 0 | CK-207 NO → cap | **2.00** | **reject** (rank 2–3, gate-capped) |
| **3** | **solution.b.md (C3 — per-graph-class forms)** | 3.52 | 0 | CK-205 NO + CK-207 NO → cap | **2.00** | **reject** (rank 2–3, gate-capped) |

All three final scores tie at the 2.0 essential-gate cap. The spec's three tie-breakers also tie (Control Honesty 4 = 4 = 4; Six-Use-Case Coverage 4 = 4 = 4; zero YES pitfall answers on all three). With the spec's tie-breakers exhausted, the residual ordering falls back to the pre-cap weighted score — the same fallback the pruning judges used for their residual ties (`.specs/research/layout-ux-2026-09-12.pruning.2.md` "Residual ties after the spec's three tie-breakers … split by pre-cap weighted"). Ranking: **A (3.88) > C (3.68) > B (3.52)**.

**overall_label: select = solution.a.md (C2).** Per the labels rule, rank 1 is select even gate-capped; ranks 2–3 are reject because both are gate-capped (adjusted score < 2.5 clause also bites). The reject labels are gate mechanics, not quality verdicts: all three documents are substantively strong (pre-cap 3.52–3.88), and each gate failure is a small, precisely fixable defect (one renamed signal field; one invalid config key in a worked YAML; one missing contract section + one fabricated config citation). The merge_map below treats C and B as full merge contributors despite the mechanical labels.

---

## Stage 2: Reference Result (generated before comparative analysis)

A correct full solution for this brief would exhibit:

1. **Six concrete walkthroughs**, each naming the rendered panel state (rows present/absent/collapsed, provenance text) — including remote engines rendered from the broker's settings JSON with no fabricated constants, and static solvers as Solve + quality + last-run with no live-sim rows. Seventh-case behavior defined.
2. **Applicability truth on every surface**: for 25/25 UFF-typed bonds, spring_len is absent/collapsed everywhere including Advanced; no raw GpuForceOptions escape hatch; vault presets quarantined from typed graphs; the vault-unit-vs-ångström collision resolved structurally (multipliers around a resolved base).
3. **Per-parameter provenance**: a why-capsule where each suppressed row would be; the false "sliders scale on top" banner (real: `layout.rs:3606–3609`) explicitly deleted, not reworded.
4. **Calm Operate-mode default**: regime/capability capsule + a few intent multipliers; raw rows in a collapsed drawer; no slop; fits 340–480px.
5. **Named-signal data flow**: `TYPED_FORCE_SUMMARY` as `Option<(usize, usize)>` = (typed_nodes, typed_edges) (`graph_canvas.rs:206`), node count from GraphSnapshot, `GpuForceOptions::for_n_nodes` (`gpu_force.rs:334`), `PanelState { active, settings, seed_strategy, seed_custom }` at `jc_layout_v1` (`layout.rs:53,76–83`), `/compute/engines` settings JSON (`layout.rs:420`), `?config=` boot presets (`appstate.rs:43–46`), authored seeding via `seed_mode: none` (`caffeine-uff.yaml:22`; SeedMode = Random/TopoFisheye/None only, `gpu_force.rs:96–119`).
6. **Concrete migration**: versioned key/schema, explicit per-field rule for v1 payloads, defined unparseable fallback, `?config=`/caffeine-uff.yaml compatibility examined.
7. **Kaizen**: phase 1 ships alone and deletes a named lie (dead spring_len slider and/or false banner); engine-crate changes, if any, last and justified.
8. **Mergeability**: shared vocabulary with the other two proposals; concrete adopt/reject position on B1 intent sliders.

## Codebase Verification (grep-verified, per `verification_duty`)

| Claim (document) | Verdict | Evidence |
|---|---|---|
| `TYPED_FORCE_SUMMARY` exists, drives banner (all three) | TRUE — but shape is `GlobalSignal<Option<(usize, usize)>>` = **(typed_nodes, typed_edges)** | `app/ui/src/graph_canvas.rs:206-207,486`; banner at `app/ui/src/panels/layout.rs:3606-3609` ("…the sliders below scale on top of them.") |
| A & B: `TYPED_FORCE_SUMMARY.bonds_typed / bonds` (A §3 line "typed_coverage = …", B §1 predicate table) | **CONTRADICTION** — the signal is a 2-tuple of counts; it has no `.bonds_typed` field and carries **no total-bonds denominator**. Renamed-API claim; coverage must be derived as `typed_edges / snapshot edge count`, which neither doc writes. | `graph_canvas.rs:206` |
| GpuForceOptions fields incl. spring_len, repulsion, seed_mode, theta, repulsion_mode (all three) | TRUE | `crates/graph-layouts/src/layout/algorithms/gpu_force.rs:173-242` |
| `for_n_nodes(n)` size tuning (all three) | TRUE | `gpu_force.rs:334` |
| PanelState at `jc_layout_v1` = `{ active, settings, seed_strategy, seed_custom }` (all three) | TRUE | `app/ui/src/panels/layout.rs:53,76-83` |
| Remote engines via `GET /compute/engines` with per-engine settings (A, B, C) | TRUE | `layout.rs:131-133,420` |
| B: `--compute-url` broker flag | TRUE | `layout.rs:3025` |
| `?config=` boot presets via `GET /configs` (A, C) | TRUE | `app/ui/src/appstate.rs:43-46,567-571` |
| caffeine-uff.yaml pins spring_len 1.4, repulsion 0.02, grid, gravity 0, **`seed_mode: none`** (ground truth) | TRUE | `app/configs/caffeine-uff.yaml:10-22` |
| C: worked `molecular-uff.yaml` declares `seed: "authored"` under `options:` | **CONTRADICTION** — the engine option field is `seed_mode`, and `SeedMode` has no `authored` variant (Random/TopoFisheye/None only; `from_str` maps unknown → Random, i.e. this value would silently *re-seed over* authored positions if it parsed). C's own schema rule ("options must deserialize against the named engine's options struct") fails on C's own flagship example, and it contradicts the file it claims to replace (`seed_mode: none`). | `gpu_force.rs:96-143`; `caffeine-uff.yaml:22` |
| B: `why ▸` cites "authored-coordinate source (`sdf.toml seed = "authored"`)" | **CONTRADICTION** — `charts/jump-cannon/packages/sdf.toml` exists but contains **no `seed` key**; authored seeding flows via `seed_mode: none` in panel settings. A provenance line citing a nonexistent key is false provenance. | grep of `charts/jump-cannon/packages/sdf.toml` |
| C: "`serde(deny_unknown_fields)` (same discipline as importer packages)" | TRUE — pervasive in `crates/importer`, `crates/data-loader`, `crates/graph-api` | e.g. `crates/importer/src/lib.rs:81,128,146` |
| C: "`LayoutPreset::{Fast,Balanced,Pretty}` … and its detect-fingerprinting heuristic" | TRUE | `layout.rs:1078-1130, 3584` |
| A: schemars "serde schema generation already in the dependency tree" | TRUE (transitively — schemars 0.8/0.9/1.2 in `app/Cargo.lock`; adding it as a direct dep adds no new crate to the closure) | `app/Cargo.lock:4567-4598` |
| A: `include_str!` embedded manifests, build-time generation in inc 3 | Feasible, no engine change in core pattern (engine-crate derives quarantined to increment 3, justified by CK-005) | — |

Net: every document carries at least one grep-verifiable codebase contradiction, and all three are of the same class — asserting a field/key shape the ground truth documents differently.

---

## Stage 4: Checklist Results

### solution.a.md (branch: C2)

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walkthroughs 1–6 each give concrete panel state: case 2 "spring_len predicate false → collapsed capsule `25/25 rests from UFF`; Backend absent (n<500)… Keep-authored-3D toggle renders"; case 6 "Solve + quality enum, no live-sim rows… `static · last solved 12:03`" |
| CK-202 | YES | "Advanced — the manifest-filtered complete form… Dead knobs cannot appear here either (CK-202)"; data-owned dims render as collapsed capsules requiring explicit override ("Override expands it into an editing row and marks it `custom`") |
| CK-203 | YES | All mechanisms are Dioxus memos/signals + `include_str!` JSON + build-time schemars generation; no JS, no styling pipeline claims anywhere |
| CK-204 | YES | Core pattern (inc 1–2) is a hand-checked JSON manifest + panel rendering; engine-crate changes (schemars derives, capability attributes) quarantined to "Increment 3", remote manifest serving to inc 4 — later and justified |
| CK-205 | YES | All contract sections present: IA §1 + wireframe, manifest §2, "**3. Data flow**" §3 with named signals, six walkthroughs §4, "5. Persistence & migration", "6. Kaizen phasing", "7. Judge-condition responses" |
| CK-206 | YES | §7 answers both C2 conditions: "CK-006…: why-capsule + per-row `owned_by` capsules" (adopts C1's capsule, §1: "This is C1's regime capsule adopted as C2's provenance display"); "CK-005…: manifests are projections of serde schemas + colocated capability attributes" (§2.1) |
| CK-207 | **NO** | §3: "`typed_coverage = TYPED_FORCE_SUMMARY.bonds_typed / bonds`" — the real signal is `Option<(usize,usize)>` = (typed_nodes, typed_edges) (`graph_canvas.rs:206`); renamed field + invented denominator. All other integration points verified TRUE |
| CK-208 | YES | §1 wireframe ≈44 columns, four stacked sections inside the existing panel; "the panel keeps its four current sections… as visual anchors" |
| CK-209 | NO | §5 gives an explicit rule ("legacy absolutes ÷ vault base → vault-regime overrides") and keeps `?config` "untouched", but no version marker on the new schema and **no defined fallback for missing/unparseable state** |
| CK-210 | YES | §5: persists `{ engine, regime_id, overrides }`, multiplier-keyed "so vault values can't leak onto ångström geometry"; collision rule stated: override "survives regime switch only if the manifest dimension's applicability holds… otherwise parked" |
| CK-211 | YES | Manifests are JSON data embedded via `include_str!`, generated from serde schemas; "No hand-written per-engine tables" (§2.1); behavior driven by data + derivation |
| CK-212 | YES | Inc 1: "manifest for gpu-force only… panel renders spring_len capsule + backend absence + why-capsule. Kills the two measured lies. No other UI change." |
| CK-213 | YES | Capability manifests are the mechanism throughout (§2 manifest schema, §3 applicability evaluation, §4 predicate-driven walkthroughs) |
| CK-214 | YES | Adopted: "Controls default to **dimensionless multipliers** around a resolved base (adopting B1's write-through semantics…)" (§2); increment 2 lands them |
| CK-215 | NO | Seventh-case behavior defined: manifest-less remote engine → "honest fallback form… settings JSON rendered generically… `applicability unknown` header"; novel data kinds fall through predicate evaluation |
| CK-216 | NO | Resolution is explained, not magical: why-capsule shows "base molecular-uff (auto: 25/25 bonds UFF-typed)" plus per-row `owned_by` |
| CK-217 | NO | Why-capsule: "`presets fast/balanced/pretty hidden (vault-tuned)`"; walkthrough 2 removes them for typed graphs |
| CK-218 | NO | Advanced is manifest-filtered ("Dead knobs cannot appear here either"); the only escape is the explicit, `custom`-marked override capsule |
| CK-219 | NO | Every walkthrough names predicate outcomes and rendered rows; data flow names signals; no "adapts appropriately" language |

### solution.b.md (branch: C3)

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walkthroughs 1–6, each backed by a full ASCII form (§2.1–2.4): case 3 "Vault form + `generated` chip… n=144 → backend row hidden"; case 5 "Remote form, generic rendering, honesty header"; case 6 "Static form: Solve + quality + last-run" |
| CK-202 | YES | Molecular form: "No global spring_len knob exists anywhere in this form — at full coverage it is meaningless"; Advanced is a "read-mostly table of resolved UFF parameters… with per-row override toggles", not raw knobs |
| CK-203 | YES | Four forms are "~40-line compositions" of a shared Rust component kit (`slider_row`, `toggle_row`, `chip`, `why_capsule`, `run_state_line`); remote rendering type-infers from settings JSON in Rust; no JS |
| CK-204 | YES | No engine changes anywhere in the document; forms, detection, and persistence are all UI-side |
| CK-205 | **NO** | Contract requires a **data flow** section ("named signals/state driving rendering"); the document has none — §1 covers class *detection* only, and within-form row flow (what signal drives `settled ✓`, node counts, the remote settings fetch → row mapping lifecycle) is never specified. All other sections present (IA §2, walkthroughs §4, persistence §3.3, phasing §5, judge responses §6, wireframes §2) |
| CK-206 | YES | §6: "CK-010…: shared component kit; form cap at 5; codified three-sources gate"; "Hybrid classes: ≥0.5 coverage → Molecular with hybrid section; <0.5 → Vault with `uff partial` chip" (§3.1–3.2) — both C3 conditions answered |
| CK-207 | **NO** | Two contradictions: §1 "`TYPED_FORCE_SUMMARY.bonds_typed / bonds ≥ 0.5`" (renamed field + invented denominator; real shape `Option<(usize,usize)>`, `graph_canvas.rs:206`); §2.2 why-capsule cites "`sdf.toml seed = "authored"`" — no such key exists in `charts/jump-cannon/packages/sdf.toml` |
| CK-208 | YES | Four wireframes each ≈44 columns inside the existing panel frame ("┌ Layout · Vault ─…┐"); no modal/takeover surface |
| CK-209 | NO | "`jc_layout_v1` migrates into `.vault` verbatim" is an explicit rule with versioned per-class keys, but no fallback for unparseable payloads and **the `?config=` boot-preset interaction is never examined** — caffeine-uff.yaml is unmentioned anywhere in the document |
| CK-210 | NO | What/where is specified ("per-class keys `jc_layout_v2.vault`, `.molecular`…"), but the precedence among data-owned (L0) / defaults (L1) / user-set (L2) is never stated as a rule — collisions are avoided by construction (absent knobs), not resolved by a stated precedence |
| CK-211 | NO | The core mechanism is **compiled** per-class specialization: four hand-composed Rust forms plus a compiled ordered predicate table (§1); only the Remote form is data-driven (settings JSON). Bounded (cap 5), but the behavior lives in code, not configuration/data |
| CK-212 | YES | Inc 1: "Molecular form only (the measured-defect class) + class detection with manual override… Kills all three lies with one new ~150-line form" — smallest shippable increment deleting named lies |
| CK-213 | YES | Per-graph-class forms are the pattern throughout; §7 explicitly positions C3 as "the design target (what each class should see)" |
| CK-214 | YES | Adopted: "Geometry scale — … multiplies every UFF rest proportionally (B1's Spread, specialized)"; §7 "B1's intent multipliers appear as Geometry scale / Repulsion (atoms)" |
| CK-215 | NO | "Form count is hard-capped at 5… a new graph class uses the Vault form… until *three* distinct sources of that class exist" — bounded with defined seventh-class behavior |
| CK-216 | NO | Classification is visible and overridable ("Detection result is shown as the form's title chip (`Molecular ▾`); the ▾ is the manual override menu") — though unlike A/C it does not display *why* the class matched (see Provenance dimension) |
| CK-217 | NO | Molecular form has no presets row; why ▸ states "`vault presets hidden (would clobber UFF rests)`" |
| CK-218 | NO | Advanced = "read-mostly table of resolved UFF parameters… No global spring_len knob exists anywhere in this form" |
| CK-219 | NO | Forms specify exact rows, chips, sub-lines ("`scales UFF rests + 13 untyped edges`"); no hand-waving language |

### solution.c.md (branch: C1)

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walkthroughs 1–6 with resolved regime + panel state per case; remote and static additionally have worked registry YAML (§1.1 `ray-force-3d.yaml`, `force-atlas-static.yaml`) — the pruning-gate fix landed |
| CK-202 | YES | "Data-owned dimensions (option value `null` + typed coverage) render as collapsed capsules with explicit override… Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere (CK-202)" |
| CK-203 | YES | Registry is YAML data loaded through the existing `?config`/`GET /configs` machinery; panel is rsx bands; resolver is plain Rust over a 5-field record; no JS |
| CK-204 | YES | Core pattern is YAML + loader + resolver + UI; options validation happens against serde shapes UI-side ("registry load fails loudly at boot"); no engine-crate change in any increment |
| CK-205 | YES | All contract sections present: registry/IA §1–2 + wireframe, "**3. Data flow**" with named signals, six walkthroughs §4, "5. Persistence & migration", "6. Kaizen phasing", "7. Judge-condition responses" |
| CK-206 | YES | §7: "**Pruning gate (remote/static never named):** both are first-class registry citizens with worked YAML (§1.1) and walkthroughs (§4.5–6). Remote regimes declare *no constants* — schema comes from the broker" |
| CK-207 | **NO** | §1.1 worked `molecular-uff.yaml` declares `seed: "authored"` under `options:` — the real option is `seed_mode`, `SeedMode` has no `authored` variant (`gpu_force.rs:96-143`), and the file it replaces uses `seed_mode: none` (`caffeine-uff.yaml:22`). The flagship example violates its own stated schema rule. `TYPED_FORCE_SUMMARY` itself is used correctly by name/role (§3) |
| CK-208 | YES | §2 wireframe ≈44 columns, three bands inside the existing panel ("One panel, three bands") |
| CK-209 | NO | Strongest treatment of the three — versioned key (`jc_layout_v2`), explicit rule ("Legacy `jc_layout_v1` absolutes migrate into `custom` regime overrides against `vault-large`"), `?config` convergence ("one loader, one schema") — but still **no defined fallback for missing/unparseable localStorage state** |
| CK-210 | YES | Precedence is stated as composition: "`effective options = regime.options ∘ user_overrides`" with "null + data-owned dims filled from TYPED_FORCE_SUMMARY"; "Overrides are stored **by control id against the regime base**, so a regime YAML edit… propagates" |
| CK-211 | YES | The whole pattern is configuration-as-data: YAML regimes under `app/configs/regimes/`, presets migrated to YAML, "presets become deployable data, honoring packages-not-crates"; the Rust `LayoutPreset` enum "is deleted (clean cutover)" |
| CK-212 | YES | Inc 1 ships alone and "Kills the three measured lies" — but bundles registry schema + loader + resolver + capsule + collapsed rows + `?config` convergence (see Kaizen dimension; largest phase 1 of the three) |
| CK-213 | YES | Regime-as-data YAML registry is the mechanism throughout; §8 positions it as "the *data layer* of the pattern the judges converged on" |
| CK-214 | YES | Adopted: controls declare `kind: multiplier`; §8 "B1's intent multipliers are the `kind: multiplier` controls" |
| CK-215 | NO | Predicate resolution absorbs novel classes (ordered first-match, specificity-sorted; vault regimes as least-specific catch); remote covered generically (`applicability: { engine_kind: remote }`). Residual gap: the no-regime-matches case is never explicitly defined (see Issues) |
| CK-216 | NO | "The resolution result is always shown with its reason: `Molecular · UFF (auto: 25/25 bonds UFF-typed)`" |
| CK-217 | NO | `presets_hidden: [fast, balanced, pretty]   # vault-tuned; quarantined for this regime` |
| CK-218 | NO | "Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere" |
| CK-219 | NO | Resolver pseudocode, worked YAML for three regime kinds, explicit per-case resolution — no "adapts appropriately" language |

### Checklist summary

| Document | total | passed | failed | essential_failures | pitfall_triggers |
|---|---|---|---|---|---|
| solution.a.md | 19 | 17 | 2 | **1 (CK-207)** | 0 |
| solution.b.md | 19 | 14 | 5 | **2 (CK-205, CK-207)** | 0 |
| solution.c.md | 19 | 17 | 2 | **1 (CK-207)** | 0 |

---

## Stage 5: Rubric Scores

Weights: Coverage 0.16 · Honesty 0.18 · Provenance 0.12 · Operate-UX 0.12 · Feasibility 0.12 · Migration 0.12 · Kaizen 0.08 · Mergeability 0.10 (Σ=1.00).

### solution.a.md

- **criterion_name: Six-Use-Case Coverage Depth** (w 0.16)
  - reasoning: All six cases get predicate-driven panel-state walkthroughs (controls rendered/absent/capsule, regime capsule text, chips). Contrast axis: every case concrete, not deferred. Cases 3–4 are delta-style ("Same as vault-small; regime `Generated · why ▸`… No authored positions → no keep-authored toggle") — concrete but thinner than cases 1/2/5/6. Seventh case defined (fallback generic form for manifest-less engines; novel data falls through predicates).
  - anchor_comparison:
    - contrast_axis: "Every case gets a concrete panel-state walkthrough, not a deferral to existing forms."
    - closer_to: score_4 — "controls render from the engine's existing /compute/engines settings JSON; no local physics rows are constructed. A remote engine without a settings schema falls back to the same generic renderer over {}." ↔ artifact §4.5–6: "Panel renders from the served manifest exactly as local… Fallback generic form (settings JSON, `applicability unknown` header). Static one-shot solver… renders Solve + quality enum, no live-sim rows"
    - further_from: score_2 — "Generated and importer graphs are untyped, so they behave like vault. Remote engines and static solvers keep their existing forms for now." ↔ artifact §4.3–4 does assert vault-likeness for generated/importer BUT with specified regime capsules, chips, and toggle absence; remote/static get new specified renderings, not "existing forms for now"
    - lean: none — cases 3–4 are delta-style rather than full panel-state renderings, so the set does not clearly exceed the anchor
    - placement: matches score_4
  - score: 4 · weighted: 0.64
  - improvement: Expand walkthroughs 3–4 to full panel-state renderings (exact rows for a 144-node generated grid and an untyped httpjson graph), not deltas from vault-small.

- **criterion_name: Control Honesty and Applicability** (w 0.18)
  - reasoning: Contrast axis: escape surfaces obey the same applicability truth. Advanced is manifest-filtered ("Dead knobs cannot appear here either"); spring_len at full UFF coverage is a collapsed capsule with an explicit `custom`-marked override — a sanctioned, labeled exception, not a live lying knob; gravity carries a caution chip ("pulls authored coordinates to origin"); multipliers around resolved bases kill the unit collision structurally.
  - anchor_comparison:
    - contrast_axis: "Escape surfaces obey the same applicability truth, not reintroduce raw knobs."
    - closer_to: score_4 — "The advanced drawer emits only rows whose applicability predicate is live for the current graph; for 25/25 UFF-typed bonds the spring_len row is absent here too." ↔ artifact §1: "Advanced — the manifest-filtered complete form: every dimension of the engine, each either a live control or a collapsed-data capsule. Dead knobs cannot appear here either (CK-202)."
    - further_from: score_2 — "An 'Advanced' disclosure reveals the raw GpuForceOptions sliders for power users." ↔ artifact: no surface renders raw GpuForceOptions sliders; the only editing path for a data-owned dimension is the explicit override that "marks it `custom`"
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.72
  - improvement: State whether the override-capsule edit writes a multiplier over the UFF base or an absolute replacement, so the sanctioned exception cannot reintroduce a unit collision.

- **criterion_name: Provenance Legibility** (w 0.12)
  - reasoning: Contrast axis: absence explained per-parameter, banner corrected. Why-capsule expands to a full provenance table (base/rests/weights/presets/yours); per-row `owned_by` capsules; parked-override surfacing; and the false banner is explicitly addressed: "the false 'sliders scale on top' banner is gone — replaced by per-row truth."
  - anchor_comparison:
    - contrast_axis: "Absence is explained with per-parameter provenance, not left silent or re-aggregated."
    - closer_to: score_4 — "Where the spring_len row would be, a why-capsule reads '25 bond lengths from UFF import — spring length is data-owned'; the old 'sliders scale on top' banner is deleted, not reworded." ↔ artifact §1 wireframe row "`▸ 25/25 rests from UFF  (override)` ← collapsed dead dimension" + §4.2 "the false 'sliders scale on top' banner is gone"
    - further_from: score_2 — "Inapplicable controls are simply not rendered; the banner reads 'Molecular parameters active'." ↔ artifact: nothing is silently absent without a stated reason path (capability-absent rows are explained by predicates' `note` fields, e.g. "meaningless below 500 nodes"), and the banner is deleted
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.48
  - improvement: Give capability-absent rows (not just data-owned ones) a discoverable why — e.g. a collapsed "backend: n/a below 500 nodes" line — so both absence classes carry provenance.

- **criterion_name: Operate-Mode UX Quality** (w 0.12)
  - reasoning: Contrast axis: glanceable few live controls vs dense form + toggle. The default view is engine picker + regime capsule + one collapsed-data row + 3–4 multiplier rows + toggle + halt line, with the complete form inside collapsed Advanced — for both vault and molecular states. ~8–9 elements before acting; no slop (text rows, no cards/chips-as-decoration); fits the width.
  - anchor_comparison:
    - contrast_axis: "Glanceable state with few live controls, not the same dense form plus another toggle."
    - closer_to: score_4 — "Default view: regime capsule ('vault-large — 12,400 nodes') plus an intent row (spread out / settle faster); raw rows live in a collapsed drawer." ↔ artifact §1 wireframe: "`regime   Molecular · UFF · why ▸` / `Spread ●──── ×1.0` / `Settle ──●── ×1.0` / `Advanced ▸  (manifest-filtered form)`"
    - further_from: score_2 — "A 'Molecular mode' checkbox is added above the existing ten sliders and three preset buttons." ↔ artifact: no mode checkbox; absolute sliders exist only inside Advanced; presets are a manifest-declared regime list, hidden when vault-tuned
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.48
  - improvement: Name the single primary action per graph state (e.g. vault: pick regime; molecular: Geometry-scale) so "one action per screen" is designed, not emergent.

- **criterion_name: Constraint Feasibility and Data-Flow Specificity** (w 0.12)
  - reasoning: Contrast axis: named existing signals drive rendering. §3 names TYPED_FORCE_SUMMARY, node count, `for_n_nodes`, source kind, a snapshot-rev memo, write-through into engine options, and the persisted shape. All mechanisms are Dioxus-feasible; schemars verified in the lockfile. One dent: the coverage expression uses renamed/invented signal fields (`.bonds_typed / bonds`) — structurally correct, not implementable exactly as written (correctable as typed_edges ÷ snapshot edge count without redesign).
  - anchor_comparison:
    - contrast_axis: "Named existing signals drive rendering, not unspecified 'state'."
    - closer_to: score_4 — "Row emission keys off TYPED_FORCE_SUMMARY (typed_nodes, typed_edges) plus node count; remote controls render from the engine's existing /compute/engines settings JSON." ↔ artifact §3: "capability state (memo, keyed on snapshot rev): typed_coverage = TYPED_FORCE_SUMMARY… n_nodes, has_authored, engine_kind" and §2.1 remote fallback "the engine's existing settings JSON rendered generically"
    - further_from: score_2 — "The panel reads the current graph state and renders the appropriate controls." ↔ artifact: every render decision is keyed to a named predicate input and persisted shape (`PanelState stores { regime_id, overrides }`)
    - lean: none — the renamed field access keeps it from a clean pole match
    - placement: matches score_4 (with the documented field-name dent, also the CK-207 gate trigger)
  - score: 4 · weighted: 0.48
  - improvement: Write coverage as `typed_edges(TYPED_FORCE_SUMMARY) / snapshot.edges.len()` to match the real `Option<(usize,usize)>` signal.

- **criterion_name: Migration and Persistence Soundness** (w 0.12)
  - reasoning: Contrast axis: old payloads migrate with defined fallback, not discarded. Migration is an explicit reinterpretation rule (legacy absolutes ÷ vault base → vault-regime overrides), multiplier-keyed persistence prevents cross-regime leaks, and `?config` is explicitly "untouched". But there is no schema version on the new record and no defined fallback for missing/unparseable payloads; the reader is one-shot, not tolerant.
  - anchor_comparison:
    - contrast_axis: "Old payloads migrate with a defined fallback, not discarded on a version bump."
    - closer_to: score_4 — "Reader accepts v1 payloads: missing regime field → resolve from TYPED_FORCE_SUMMARY + node count; settings re-keyed per engine…" ↔ artifact §5: "legacy absolutes ÷ vault base → vault-regime overrides; molecule users keep `?config` boot path untouched" (explicit v1 rule, historic-base preserved)
    - further_from: score_2 — "We bump the localStorage key to jc_layout_v2; old settings are discarded." ↔ artifact: nothing is discarded; yet the score_4 anchor's second half — "unparseable → for_n_nodes fallback. Writer emits v2 with schema: 2" — has no counterpart: artifact lacks any version marker and any unparseable fallback
    - lean: none
    - placement: past score_2, short of score_4
  - score: 3 · weighted: 0.36
  - improvement: Add `schema: 2` to the written record and state the fallback (unparseable → default `for_n_nodes(n)` vault regime, preserving today's `unwrap_or_default` behavior).

- **criterion_name: Kaizen Phasing** (w 0.08)
  - reasoning: Contrast axis: phase 1 ships alone and deletes a named lie. Inc 1 is one engine's hand-checked manifest + capsule + absence + why-capsule, "Kills the two measured lies. No other UI change." Later increments each add one layer (multipliers → derivation infra → remote/static); the engine-crate change is inc 3 and argued via CK-005.
  - anchor_comparison:
    - contrast_axis: "Phase 1 ships alone and deletes a named lie, not bundles the whole design."
    - closer_to: score_4 — "Phase 1 (ships alone): truthful banner + applicability-filtered rows for the local engine — the lying spring_len slider is deleted." ↔ artifact §6: "Increment 1 (smallest): manifest for gpu-force only… panel renders spring_len capsule + backend absence + why-capsule. Kills the two measured lies."
    - further_from: score_2 — "Phase 1: implement the manifest schema, the renderer, the migration, and the new presets." ↔ artifact: migration is inc 2, derivation infra inc 3, remote inc 4 — explicitly not bundled
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.32
  - improvement: State explicitly that inc 1 deletes the false banner (it is implied by "two measured lies" but the banner's removal is otherwise only visible in §4.2).

- **criterion_name: Mergeability and Synthesis** (w 0.10)
  - reasoning: Contrast axis: shared vocabulary enabling the C1+C2+B1 merge. §8 names the merge structure precisely: C1's `regimes/*.yaml` feeds the manifest `regimes` array + why-capsule content; C3's forms are "the renderings this manifest produces under those capability states — C2 is the mechanism, C3 the observed shapes"; B1 adopted as the default control kind with write-through semantics. Why-capsule explicitly credited to C1.
  - anchor_comparison:
    - contrast_axis: "Shared vocabulary enabling the C1+C2+B1 merge, not a siloed design."
    - closer_to: score_4 — "The regime capsule and the capability manifest are one applicability record… B1 intent sliders are adopted… as dimensionless multipliers writing through into GpuForceOptions." ↔ artifact §8 + §2: "Controls default to **dimensionless multipliers** around a resolved base (adopting B1's write-through semantics…)" and §1 "This is C1's regime capsule adopted as C2's provenance display — the merge point both sets of judges named."
    - further_from: score_2 — "(The document never mentions the other proposals, the one-pattern observation, or intent sliders.)" ↔ artifact has a dedicated §8 Mergeability section naming C1, C3, and B1 with integration shapes
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.40
  - improvement: Define the shared record's field-level mapping (manifest dimension ↔ regime `controls` entry ↔ why-capsule line) so the merge is a mechanical join, not a re-negotiation.

**solution.a.md raw weighted sum: 3.88** → essential gate CK-207 NO → **final 2.00** (pitfalls: none).

### solution.b.md

- **criterion_name: Six-Use-Case Coverage Depth** (w 0.16)
  - reasoning: Contrast axis: concrete walkthrough per case, not deferral. All six cases resolve to a fully wireframed form (§2.1–2.4) with per-case deltas (§4); remote and static get purpose-built forms — the exact opposite of "keep their existing forms for now". Cases 1/3/4 intentionally share the Vault form, but that form is specified (n-tuned tags, per-class chips, n<500 backend rule) — absorption by mechanism, not deferral. Seventh-class behavior is the best-defined of the three (Vault default + three-sources gate + cap 5).
  - anchor_comparison:
    - contrast_axis: "Every case gets a concrete panel-state walkthrough, not a deferral to existing forms."
    - closer_to: score_4 — "controls render from the engine's existing /compute/engines settings JSON; no local physics rows are constructed. A remote engine without a settings schema falls back to the same generic renderer over {}." ↔ artifact §2.3: "Controls are type-inferred from the engine's settings JSON… `settings as declared by <engine>` — no vault presets, no n-tuned claims, no provenance capsules the broker didn't supply."
    - further_from: score_2 — "Generated and importer graphs are untyped, so they behave like vault. Remote engines and static solvers keep their existing forms for now." ↔ artifact: generated/importer DO route to the Vault form (the score_2 sentence's first half is B's actual mechanism — but with specified chips, tags, and n-rules), and remote/static get new specified forms (§2.3, §2.4), directly negating the anchor's second half
    - lean: none — walkthroughs (§4) are one-liners whose concreteness lives in the referenced forms; full panel state per case exists but is assembled by reference
    - placement: matches score_4
  - score: 4 · weighted: 0.64
  - improvement: Make each §4 walkthrough self-contained (render the resolved form with the case's deltas inline) so coverage does not depend on cross-referencing §2.

- **criterion_name: Control Honesty and Applicability** (w 0.18)
  - reasoning: Contrast axis: escape surfaces obey the same truth. Molecular form contains no spring_len knob anywhere ("at full coverage it is meaningless, at partial coverage Geometry scale governs the remainder and says so — `scales UFF rests + 13 untyped edges`"); Advanced is a read-mostly resolved-UFF table with explicit per-row override toggles; the Remote form's header is an honesty contract with no fabricated claims; the Vault form's knobs are all genuinely live for its class. Residual: below-50% typed graphs show Vault's spring_len while 12/25 bonds carry UFF rests — same semantics as today, disclosed by the `uff partial` chip.
  - anchor_comparison:
    - contrast_axis: "Escape surfaces obey the same applicability truth, not reintroduce raw knobs."
    - closer_to: score_4 — "The advanced drawer emits only rows whose applicability predicate is live for the current graph; for 25/25 UFF-typed bonds the spring_len row is absent here too." ↔ artifact §2.2: "Advanced ▸ — read-mostly table of resolved UFF parameters (per-bond rest, per-atom weight) with per-row override toggles. No global spring_len knob exists anywhere in this form."
    - further_from: score_2 — "An 'Advanced' disclosure reveals the raw GpuForceOptions sliders for power users." ↔ artifact: no raw-options surface exists in any form; the only raw constants shown (Vault form) are honestly live for that class
    - lean: none — the sub-50% residual (spring_len live while 12 bonds are UFF-typed) is disclosed but still a partially-dead knob by the measured standard
    - placement: matches score_4 (residual noted; disclosed, chip-linked, and the class threshold is a stated design choice)
  - score: 4 · weighted: 0.72
  - improvement: For sub-50% hybrid graphs, tag the Vault spring_len row with its scope ("governs 13 untyped edges") instead of relying on the `uff partial` chip alone.

- **criterion_name: Provenance Legibility** (w 0.12)
  - reasoning: Contrast axis: absence explained per-parameter, banner corrected. B removes the false banner and explains *presets* per-row (`vault presets hidden (would clobber UFF rests)`), adds per-row tags (`n-tuned`) and scope sub-lines — past score_2. But inapplicable *controls* are silently absent in the Molecular form (no capsule where spring_len would be); provenance is one aggregate `why ▸` capsule (UFF summary, elements, bond histogram) rather than per-parameter why-text; the class-detection reason is never displayed (unlike A/C's "auto: …" lines); and one provenance line is **fabricated** (`sdf.toml seed = "authored"` — no such key), which is false provenance on the exact axis this dimension measures.
  - anchor_comparison:
    - contrast_axis: "Absence is explained with per-parameter provenance, not left silent or re-aggregated."
    - closer_to: score_2 — "Inapplicable controls are simply not rendered; the banner reads 'Molecular parameters active'." ↔ artifact §2.2: spring_len is simply not rendered in the Molecular form, and the header line is an aggregate summary — "`UFF  25/25 bonds · 24/24 atoms · why ▸`" — structurally the score_2 pattern (absence + aggregate banner), though truthful and expandable
    - further_from: score_4 — "Where the spring_len row would be, a why-capsule reads '25 bond lengths from UFF import — spring length is data-owned'; the old 'sliders scale on top' banner is deleted, not reworded." ↔ artifact lacks: any per-parameter capsule at the suppressed row's position; the banner's deletion is explained ("it was false only for typed graphs, but per-class forms make it unnecessary everywhere") yet the per-parameter replacement the anchor demands is absent; and the why-capsule contains one fabricated citation (`sdf.toml seed = "authored"`)
    - lean: toward score_4 — truthful aggregate + per-row tags + preset-quarantine line + scope sub-lines sit clearly above silent absence, but the missing per-parameter why and the fabricated citation block the pole
    - placement: past score_2, short of score_4, leaning score_4
  - score: 3 · weighted: 0.36
  - improvement: Add a collapsed why-capsule at each suppressed dimension's position (A's pattern), display the detection reason on the title chip, and delete the invented `sdf.toml seed = "authored"` citation.

- **criterion_name: Operate-Mode UX Quality** (w 0.12)
  - reasoning: Contrast axis: glanceable few live controls vs dense form + toggle. The Molecular form is the calmest default of all three documents (~7 elements: engine line, UFF chip, 2 sliders, toggle, settled line, Advanced) and there is no mode toggle anywhere. But the **Vault form — the default use case (case 1)** — is "essentially today's panel": presets row + 4 sliders + cooling line + backend/seed line + halt line ≈ 11+ elements, the same dense form the doctrine measures against, kept deliberately. A mixed instance set: matches score_4 for molecular, sits near the dense-form pole for vault (without the toggle, with honest tags).
  - anchor_comparison:
    - contrast_axis: "Glanceable state with few live controls, not the same dense form plus another toggle."
    - closer_to: score_4 — "Default view: regime capsule ('vault-large — 12,400 nodes') plus an intent row (spread out / settle faster); raw rows live in a collapsed drawer." ↔ artifact §2.2 Molecular form: "`Geometry scale ───●── ×1.0` / `Repulsion ──●─ ×1.0 (atoms)` / `☑ Keep authored 3D` / `settled ✓ … [wake]` / `Advanced ▸`" — the anchor's shape exactly
    - further_from: score_2 — "A 'Molecular mode' checkbox is added above the existing ten sliders and three preset buttons." ↔ artifact §2.1: the Vault form reproduces the dense ten-slider-plus-presets layout ("presets [fast] [balanced●] [pretty] / spring len … 53.6 / spring k … 60 / repulsion … 535 / gravity … 0.05 / cooling … backend … halt …") — the dense half of the score_2 picture, minus the checkbox
    - lean: none — the two default states sit at opposite poles and neither dominates; the vault default is the most frequent case
    - placement: past score_2, short of score_4
  - score: 3 · weighted: 0.36
  - improvement: Apply the molecular discipline to the Vault form — demote cooling/backend/halt absolutes into the Advanced drawer and surface 2–3 intent multipliers as the vault default.

- **criterion_name: Constraint Feasibility and Data-Flow Specificity** (w 0.12)
  - reasoning: Contrast axis: named signals, not unspecified "state". Detection is specified against named sources (GraphSnapshot, TYPED_FORCE_SUMMARY, engine kind, `--compute-url` verified real), remote rendering names the settings-JSON type-inference mapping, defaults name `for_n_nodes`. But the document has **no data-flow section**: within-form flow (what signal drives `settled ✓ (halt 0.08, 0.4s)`, node-count lines, the settings-fetch → row lifecycle, stale/disconnected states beyond one chip) is unspecified, and the detection predicate repeats the renamed-field error (`.bonds_typed / bonds`). Everything claimed is buildable in Dioxus with existing components; specificity is thinner than A/C.
  - anchor_comparison:
    - contrast_axis: "Named existing signals drive rendering, not unspecified 'state'."
    - closer_to: score_4 — "Row emission keys off TYPED_FORCE_SUMMARY (typed_nodes, typed_edges) plus node count; remote controls render from the engine's existing /compute/engines settings JSON." ↔ artifact §1 predicate table ("`TYPED_FORCE_SUMMARY.bonds_typed / bonds ≥ 0.5`…", "engine kind is remote") and §2.3 ("Controls are type-inferred from the engine's settings JSON (number→slider within declared bounds, bool→toggle, enum→select)")
    - further_from: score_2 — "The panel reads the current graph state and renders the appropriate controls." ↔ artifact: form *selection* is fully signal-specified; but row-level rendering flow inside forms is closer to the score_2 sentence — e.g. `settled ✓ (halt 0.08, 0.4s)` and `cluster: 2×A100 · queue ok` appear in wireframes with no named signal or state source anywhere in the document
    - lean: toward score_4 — the central rendering decision (which form) is named-signal-driven, but the missing data-flow section leaves real surfaces un sourced
    - placement: past score_2, short of score_4, leaning score_4
  - score: 3 · weighted: 0.36
  - improvement: Add the contract-required data-flow section: per-form table of row → driving signal/state (run-state line, node counts, settings fetch lifecycle, stale cache).

- **criterion_name: Migration and Persistence Soundness** (w 0.12)
  - reasoning: Contrast axis: migrate with defined fallback, not discard. The v1 → `.vault` verbatim rule is concrete, lossless, and honors historic meaning; per-class versioned keys fix the real cross-class clobber defect. But there is no unparseable/missing fallback, and the `?config=` boot-preset path (caffeine-uff.yaml) is never examined — a named ground-truth integration point the migration must not regress, unmentioned anywhere in the document.
  - anchor_comparison:
    - contrast_axis: "Old payloads migrate with a defined fallback, not discarded on a version bump."
    - closer_to: score_4 — "Reader accepts v1 payloads… settings re-keyed per engine…" ↔ artifact §3.3: "`jc_layout_v1` migrates into `.vault` verbatim (its historic meaning). Per-class settings persist under per-class keys (`jc_layout_v2.vault`, `.molecular`, …) so switching class never clobbers the other class's tuning"
    - further_from: score_2 — "We bump the localStorage key to jc_layout_v2; old settings are discarded." ↔ artifact: nothing is discarded (verbatim migration); yet artifact lacks: the anchor's "unparseable → for_n_nodes fallback" and any `?config=` preset interaction — the anchor's exact v1 shape test (`?config= boot presets like caffeine-uff.yaml`) is unaddressed
    - lean: none
    - placement: past score_2, short of score_4
  - score: 3 · weighted: 0.36
  - improvement: Specify the unparseable fallback and state how a `?config=caffeine-uff` boot writes the new per-class keys (which class slot, which fields).

- **criterion_name: Kaizen Phasing** (w 0.08)
  - reasoning: Contrast axis: smallest phase 1 that ships alone and deletes a named lie. Inc 1 is the smallest of all three documents: one ~150-line Molecular form + detection + override, "Kills all three lies with one new form", everything else deferred (persistence inc 2, remote inc 3, static inc 4 — each small, ordered by value, no engine work anywhere).
  - anchor_comparison:
    - contrast_axis: "Phase 1 ships alone and deletes a named lie, not bundles the whole design."
    - closer_to: score_4 — "Phase 1 (ships alone): truthful banner + applicability-filtered rows for the local engine — the lying spring_len slider is deleted." ↔ artifact §5: "Increment 1: Molecular form only (the measured-defect class) + class detection with manual override; other classes keep today's panel as 'Vault form'. Kills all three lies with one new ~150-line form."
    - further_from: score_2 — "Phase 1: implement the manifest schema, the renderer, the migration, and the new presets." ↔ artifact: no schema/renderer/migration bundling — inc 1 is a single additive form with detection
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.32
  - improvement: None material; optionally name the inc-1 banner deletion explicitly (it is currently implied by "all three lies").

- **criterion_name: Mergeability and Synthesis** (w 0.10)
  - reasoning: Contrast axis: shared vocabulary, not a silo. §7 frames C3 as "the design target (what each class should see), C2 the engine for getting there"; identifies the predicate-ruleset identity with C1 ("class detection predicates and C1's regime-resolution predicates are the same ordered-ruleset"); the `why ▸` is credited as "C1's regime capsule"; B1 appears concretely as Geometry scale / Repulsion (atoms); and the Remote form "converges upward rather than forks" when an engine serves A's manifest.
  - anchor_comparison:
    - contrast_axis: "Shared vocabulary enabling the C1+C2+B1 merge, not a siloed design."
    - closer_to: score_4 — "The regime capsule and the capability manifest are one applicability record; C1's registry is the same data with a resolver. B1 intent sliders are adopted… as dimensionless multipliers writing through into GpuForceOptions." ↔ artifact §7: "the Molecular form's `why ▸` is C1's regime capsule… B1's intent multipliers appear as Geometry scale / Repulsion (atoms) in the Molecular form"; §2.3 "If the engine *does* serve a capability manifest (Solution A's JSON), the Remote form upgrades… the forms converge upward rather than fork."
    - further_from: score_2 — "(The document never mentions the other proposals, the one-pattern observation, or intent sliders.)" ↔ artifact: dedicated §7 Mergeability section naming C1, C2, B1 with integration shapes
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.40
  - improvement: State which artifact owns the shared component kit and predicate record in a merged implementation (B claims the forms; C claims the same predicates) to pre-empt dual ownership.

**solution.b.md raw weighted sum: 3.52** → essential gates CK-205 NO + CK-207 NO → **final 2.00** (pitfalls: none).

### solution.c.md

- **criterion_name: Six-Use-Case Coverage Depth** (w 0.16)
  - reasoning: Contrast axis: concrete walkthrough per case, not deferral. All six cases resolve to a named regime with panel state specified; remote and static — the pruning-gate failure — are covered twice over (worked YAML in §1.1 plus walkthroughs §4.5–6), and remote regimes "declare NO constants of their own" (broker schema rendered generically). Cases 3–4 are brief but concrete (resolved regime + note line). Seventh-case behavior is predicate absorption, but the no-regime-matches fallback is never explicitly defined.
  - anchor_comparison:
    - contrast_axis: "Every case gets a concrete panel-state walkthrough, not a deferral to existing forms."
    - closer_to: score_4 — "controls render from the engine's existing /compute/engines settings JSON; no local physics rows are constructed. A remote engine without a settings schema falls back to the same generic renderer over {}." ↔ artifact §4.5: "the matching registry entry declares `settings_schema: broker` — panel renders the broker's settings JSON generically under the header `settings as declared by ray-force-3d`; no Jump Cannon constants fabricated."
    - further_from: score_2 — "Generated and importer graphs are untyped, so they behave like vault. Remote engines and static solvers keep their existing forms for now." ↔ artifact §4.3–6: generated gets its own `generated.yaml` regime with a note; remote and static are first-class registry entries with worked YAML — new specified behavior, not "existing forms for now"
    - lean: none — the undefined empty-match resolution keeps the seventh-case story from clearly exceeding the anchor
    - placement: matches score_4
  - score: 4 · weighted: 0.64
  - improvement: Define the empty-match resolution explicitly (fall through to the least-specific vault regime, or a built-in default entry) and what happens when a remote engine has no registry entry at all.

- **criterion_name: Control Honesty and Applicability** (w 0.18)
  - reasoning: Contrast axis: escape surfaces obey the same truth. Data-owned dims are `null` in options and render as collapsed capsules with explicit override — in the default bands AND in Advanced ("dead knobs unrepresentable everywhere"); vault presets are quarantined per-regime (`presets_hidden`); remote surfaces render only the broker's schema under an honesty header; static surfaces expose only the quality enum. The vault regime keeps absolute sliders, which are honestly live for that class.
  - anchor_comparison:
    - contrast_axis: "Escape surfaces obey the same applicability truth, not reintroduce raw knobs."
    - closer_to: score_4 — "The advanced drawer emits only rows whose applicability predicate is live for the current graph; for 25/25 UFF-typed bonds the spring_len row is absent here too." ↔ artifact §2: "Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere (CK-202)"; walkthrough 2: "spring_len null → capsule"
    - further_from: score_2 — "An 'Advanced' disclosure reveals the raw GpuForceOptions sliders for power users." ↔ artifact: no raw-options surface; Advanced is the regime's own declared option set with data-owned entries collapsed
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.72
  - improvement: Fix the `seed: "authored"` entry (→ `seed_mode: none`); as written the flagship regime's own options violate its schema rule and would re-seed over authored positions if parsed leniently.

- **criterion_name: Provenance Legibility** (w 0.12)
  - reasoning: Contrast axis: absence explained per-parameter, banner corrected. The regime line always carries its resolution reason ("auto: 25/25 bonds UFF-typed"); data-owned rows render as capsules at the row's position; `presets_hidden` is declared per regime. Past score_2 clearly. But the document **never addresses the false banner**: it claims increment 1 "Kills the three measured lies" while saying "Today's sliders otherwise unchanged" — and the banner (layout.rs:3606–3609) is part of today's panel; no sentence deletes, rewords, or replaces it. The spec instruction requires the document to explicitly correct the "sliders scale on top" claim; silence on the artifact's fate is a miss on the exact axis.
  - anchor_comparison:
    - contrast_axis: "Absence is explained with per-parameter provenance, not left silent or re-aggregated."
    - closer_to: score_4 — "Where the spring_len row would be, a why-capsule reads '25 bond lengths from UFF import — spring length is data-owned'…" ↔ artifact §2 wireframe: "`▸ 25/25 rests from UFF    (override)   ← data-owned dimension, collapsed`" directly at the row's position, plus the regime reason line "`auto: 25/25 bonds UFF-typed`"
    - further_from: score_2 — "Inapplicable controls are simply not rendered; the banner reads 'Molecular parameters active'." ↔ artifact: absence is explained (capsule + reason), BUT the banner half of score_4 — "the old 'sliders scale on top' banner is deleted, not reworded" — has no counterpart: artifact lacks any mention of the existing false banner's fate despite claiming to kill it in increment 1
    - lean: toward score_4 — per-parameter capsules + resolution reason are present; only the explicit banner correction is missing
    - placement: past score_2, short of score_4, leaning score_4
  - score: 3 · weighted: 0.36
  - improvement: Add one sentence to §2/§6: the `lay-hint` banner is deleted in increment 1, its content superseded by the regime reason line and collapsed-data capsules.

- **criterion_name: Operate-Mode UX Quality** (w 0.12)
  - reasoning: Contrast axis: glanceable few live controls vs dense form + toggle. The three-band structure with regime-declared controls is calm for molecular (capsule + 2 multipliers + toggle + run line + Advanced) and the resolution-reason line is genuinely glanceable provenance. But "vault regimes reproduce today's behavior exactly when resolved — today's panel is the special case where the regime declares the familiar absolute sliders": the default (vault) state keeps the dense absolute-slider layout, not intent rows with raw rows in a drawer. Same mixed-instance placement as B.
  - anchor_comparison:
    - contrast_axis: "Glanceable state with few live controls, not the same dense form plus another toggle."
    - closer_to: score_4 — "Default view: regime capsule ('vault-large — 12,400 nodes') plus an intent row (spread out / settle faster); raw rows live in a collapsed drawer." ↔ artifact §2: the molecular default matches ("`regime [Molecular · UFF ▾]` + reason line + `Geometry scale ───●── ×1.0` + `Repulsion ──●─ ×1.0 (atoms)` + `Advanced ▸`"), and the regime band gives the vault default a capsule-with-reason header
    - further_from: score_2 — "A 'Molecular mode' checkbox is added above the existing ten sliders and three preset buttons." ↔ artifact §2: "The vault regimes (`fast/balanced/pretty` + `vault-large/small`) reproduce today's behavior exactly when resolved — today's panel is the special case where the regime declares the familiar absolute sliders" — the dense vault form persists by design (no mode checkbox, but the dense form itself)
    - lean: none
    - placement: past score_2, short of score_4
  - score: 3 · weighted: 0.36
  - improvement: Ship a vault intent regime (2–3 multiplier controls over the `for_n_nodes` base) as the resolved default for large untyped graphs, demoting absolutes to Advanced — the registry makes this a data change, not code.

- **criterion_name: Constraint Feasibility and Data-Flow Specificity** (w 0.12)
  - reasoning: Contrast axis: named existing signals. §3 is the strongest data-flow pipeline of the three: registry load (with `deny_unknown_fields`, verified as importer-package discipline) → 5-field snapshot record → resolver → `effective options = regime.options ∘ user_overrides` with "null + data-owned dims filled from TYPED_FORCE_SUMMARY" (correct name and role) → render → `jc_layout_v2`. The registry rides the existing `/configs` serving path. One dent: the flagship YAML's `seed: "authored"` contradicts the real `seed_mode: none` field/value (CK-207 gate) and would fail its own validation rule.
  - anchor_comparison:
    - contrast_axis: "Named existing signals drive rendering, not unspecified 'state'."
    - closer_to: score_4 — "Row emission keys off TYPED_FORCE_SUMMARY (typed_nodes, typed_edges) plus node count; remote controls render from the engine's existing /compute/engines settings JSON." ↔ artifact §3: "Snapshot state = `{ typed_bond_coverage, n_nodes, has_authored, source_kind, engine_kind }`… null + data-owned dims filled from TYPED_FORCE_SUMMARY"; §4.5 "panel renders the broker's settings JSON generically"
    - further_from: score_2 — "The panel reads the current graph state and renders the appropriate controls." ↔ artifact: every stage is named (registry files, resolver record, composition operator, persistence key) — nothing rides on unspecified "state"; the single dent is the invalid `seed` key in the worked YAML
    - lean: none — at the pole on specificity, carrying the documented field-name defect
    - placement: matches score_4 (with the documented YAML defect, also the CK-207 gate trigger)
  - score: 4 · weighted: 0.48
  - improvement: Correct `seed: "authored"` → `seed_mode: none` in `molecular-uff.yaml` and add a registry self-test: every shipped regime's `options` must deserialize against its engine's UI settings struct at boot.

- **criterion_name: Migration and Persistence Soundness** (w 0.12)
  - reasoning: Contrast axis: migrate with defined fallback, not discard. The most complete migration story of the three: versioned key (`jc_layout_v2 = { regime_id | "custom", overrides }`), explicit v1 rule ("absolutes migrate into `custom` regime overrides against `vault-large` (their historic base)"), `?config=` convergence designed rather than ignored ("boot preset = registry YAML referenced by URL… one loader, one schema"), caffeine-uff.yaml's path named, and the LayoutPreset enum deleted as clean cutover. Missing only the unparseable/missing-payload fallback. One flagged risk: the persisted spring_len anchor that graph load uses for authored-coordinate rescale (layout.rs:110–114) is redesigned to `null` without addressing that consumer [INFERENCE — the interaction is real per the code comment, the breakage severity is my inference].
  - anchor_comparison:
    - contrast_axis: "Old payloads migrate with a defined fallback, not discarded on a version bump."
    - closer_to: score_4 — "Reader accepts v1 payloads: missing regime field → resolve from TYPED_FORCE_SUMMARY + node count; settings re-keyed per engine; … Writer emits v2 with schema: 2." ↔ artifact §5: "`jc_layout_v2`: `{ regime_id, overrides }`. Legacy `jc_layout_v1` absolutes migrate into `custom` regime overrides against `vault-large` (their historic base)"; "the `?config` loader accepts any registry id or path — one loader, one schema"
    - further_from: score_2 — "We bump the localStorage key to jc_layout_v2; old settings are discarded." ↔ artifact: nothing is discarded; but artifact lacks the anchor's "unparseable → for_n_nodes fallback" — no missing/unparseable handling is defined for the localStorage record
    - lean: toward score_4 — every anchor element present except the fallback; clearly nearer the score_4 pole than mid-interval
    - placement: past score_2, short of score_4, leaning score_4 → 4
  - score: 4 · weighted: 0.48
  - improvement: Define the unparseable/missing fallback (default regime resolution from the 5-field record) and address the authored-rescale spring_len anchor consumer before setting `spring_len: null`.

- **criterion_name: Kaizen Phasing** (w 0.08)
  - reasoning: Contrast axis: smallest phase 1 that ships alone and deletes a named lie. Inc 1 does ship alone and claims all three lies — but it bundles a new config subsystem: registry schema + loader + validation + resolver + `molecular-uff.yaml` + regime capsule + collapsed-data rows + `?config` convergence. That is infrastructure-plus-UI in one increment, well past "truthful banner + filtered rows"; a smaller inc 1 exists (capsule + banner deletion + spring_len collapse against today's hardcoded panel, registry in inc 2). Later increments are well-ordered (presets/enum deletion → multiplier controls → remote/static).
  - anchor_comparison:
    - contrast_axis: "Phase 1 ships alone and deletes a named lie, not bundles the whole design."
    - closer_to: score_2 — "Phase 1: implement the manifest schema, the renderer, the migration, and the new presets." ↔ artifact §6: "Increment 1: registry schema + loader + `molecular-uff.yaml` + resolver covering only the typed-coverage predicate; panel gains regime capsule + collapsed-data rows. Today's sliders otherwise unchanged… `?config` converges onto the schema." — schema + loader + resolver + UI + config convergence is the same bundling shape, though migration and presets are deferred to inc 2
    - further_from: score_4 — "Phase 1 (ships alone): truthful banner + applicability-filtered rows for the local engine — the lying spring_len slider is deleted." ↔ artifact lacks: a minimal first increment; the smallest lie-killing step (capsule + banner deletion against the existing panel) is not separable from the registry infrastructure in C's plan
    - lean: toward score_2 — the phase-1 bundle is the dominant fact; the deferred migration/presets keep it off the pole
    - placement: past score_2, short of score_4, leaning score_2
  - score: 3 · weighted: 0.24
  - improvement: Split inc 1: (1a) regime capsule + collapsed-data rows + banner deletion driven by a hardcoded molecular-uff record; (1b) the YAML registry/loader/resolver replacing the hardcoded record.

- **criterion_name: Mergeability and Synthesis** (w 0.10)
  - reasoning: Contrast axis: shared vocabulary enabling the merge. §8 is the most explicit one-pattern articulation of the three: "C1 is the *data layer* of the pattern the judges converged on: C2's capability manifests slot in as the per-engine `controls`/applicability truth (a manifest is the engine-side half of a regime entry); B's four forms are what this registry renders for its four engine/execution combinations; B1's intent multipliers are the `kind: multiplier` controls." Also claims the predicate record is shared with B "by design" and adopts A2's three-band structure as its rendering. Concrete contribution list for synthesis (registry + resolver, schema convergence, preset migration, override-against-regime persistence).
  - anchor_comparison:
    - contrast_axis: "Shared vocabulary enabling the C1+C2+B1 merge, not a siloed design."
    - closer_to: score_4 — "The regime capsule and the capability manifest are one applicability record; C1's registry is the same data with a resolver. B1 intent sliders are adopted… as dimensionless multipliers writing through into GpuForceOptions." ↔ artifact §8 (quoted above) — the anchor sentence is nearly a paraphrase of C's §8; §1.2 "this is the same predicate record as Solution B's class detection; they share one implementation"
    - further_from: score_2 — "(The document never mentions the other proposals, the one-pattern observation, or intent sliders.)" ↔ artifact: §8 Mergeability names C2, B (C3), B1, and A2 with integration shapes
    - lean: n/a (at pole)
    - placement: matches score_4
  - score: 4 · weighted: 0.40
  - improvement: The "manifest is the engine-side half of a regime entry" claim needs the field-level half: show one merged record carrying both C2's `applicability` block and C1's `controls` list.

**solution.c.md raw weighted sum: 3.68** → essential gate CK-207 NO → **final 2.00** (pitfalls: none).

---

## Stage 6: Score Calculation

| Document | raw weighted sum | pitfall penalties (CK-215–219 YES) | essential gate (spec `scoring.essential_gate`) | gate source | final |
|---|---|---|---|---|---|
| solution.a.md | 3.88 | −0.00 (0 pitfalls) | CK-207 NO → cap 2.0 | **specification `gates` block** (essential_gate; spec defines no important-level caps, so judge built-in caps do not apply) | **2.00** |
| solution.b.md | 3.52 | −0.00 (0 pitfalls) | CK-205 NO, CK-207 NO → cap 2.0 | specification `gates` block | **2.00** |
| solution.c.md | 3.68 | −0.00 (0 pitfalls) | CK-207 NO → cap 2.0 | specification `gates` block | **2.00** |

Tie-break per spec `scoring.ranking`: (1) Control Honesty 4=4=4; (2) Coverage 4=4=4; (3) pitfalls 0=0=0 — exhausted. Residual ordering by pre-cap weighted score (precedent: pruning.2.md residual-tie handling): **A 3.88 > C 3.68 > B 3.52**.

Labels per spec `scoring.labels`: rank 1 → **select**; ranks 2–3 gate-capped → **reject**.

## merge_map (into primary: solution.a.md — C2 capability manifests)

**From solution.c.md (C1):**
1. **The YAML regime registry + resolver** (`app/configs/regimes/*.yaml`, ordered specificity-sorted first-match over the closed 5-field snapshot record) as the data source for A's manifest `regimes` array and regime picker — the merge A §8 already anticipates ("converging toward C1's registry if merged").
2. **`?config=` schema convergence** — one loader, one schema for boot presets and regimes; the `?config` loader accepting any registry id or path. A only says "?config untouched"; C's unification is strictly better.
3. **Presets-as-YAML migration with `presets_hidden` quarantine** — Fast/Balanced/Pretty become data with per-regime visibility rules, replacing the `LayoutPreset` enum + `detect` fingerprinting (verified real, layout.rs:1078–1130, 3584) as a clean cutover.
4. **Override-against-regime-base persistence** — overrides keyed by control id against a named base so base tuning propagates as deployable data; extends A's `{ regime_id, overrides }` with C's composition semantics (`effective = regime.options ∘ user_overrides`).
5. **The resolution-reason display** on the regime line ("auto: 25/25 bonds UFF-typed") — fold into A's why-capsule header.

**From solution.b.md (C3):**
1. **The bounded-form governance rule** — hard cap (5) + three-distinct-sources gate before any new specialization is written; applies to A's manifest count as the anti-proliferation discipline (CK-010/CK-215).
2. **The hybrid-coverage sub-line pattern** — "scales UFF rests + 13 untyped edges": the standard truth-text for A's multiplier rows at partial typed coverage (A's manifest `note` field should carry exactly this).
3. **The Molecular form as the render target** — B §2.2 is the concrete acceptance shape for what A's gpu-force manifest must emit under full UFF coverage (Geometry scale + Repulsion (atoms) + Keep authored + settled line).
4. **The Static form's quality-enum mapping** (`quality → iterations/theta` table) as the standard `one_shot` manifest control shape for A's `execution: one_shot` engines.
5. **The per-class persistence keys** idea (`jc_layout_v2.<class>`) as the fix for cross-class tuning clobber, composed with C's override-against-base model.

**B1 intent sliders:** already adopted by the primary (A §2, increment 2 — dimensionless multipliers writing through into GpuForceOptions); no merge action needed beyond keeping C's `kind: multiplier` vocabulary for the shared record.

---

## Stage 7: Rule Generation

Five Whys on the three gate-triggering issues (A/B `TYPED_FORCE_SUMMARY.bonds_typed / bonds`; B `sdf.toml seed = "authored"`; C `seed: "authored"`):

1. Why: each document asserted a codebase field/key shape that does not exist. Evidence: greps above (`graph_canvas.rs:206`, `sdf.toml`, `gpu_force.rs:96–143`, `caffeine-uff.yaml:22`).
2. Why: the authors wrote integration claims from the ground-truth summary's vocabulary without checking the actual definitions — the context doc names `TYPED_FORCE_SUMMARY (typed_nodes, typed_edges)` correctly, yet two documents re-invented it as `.bonds_typed / bonds`, and two invented seed keys instead of reusing the verified `seed_mode: none`.
3. Why: no step in the expansion workflow forced a grep of each asserted identifier before the document was finalized.
4. Why: the repo has a demonstrated norm of grep-verification (pruning judges' "Verified against the codebase" section caught exactly this class at proposal stage), but it lives in judge practice, not in any standing instruction an author agent loads.
5. Root cause: **missing systemic guardrail** — no rule requires agents to grep-verify every codebase identifier (signal, field, config key, enum variant) before asserting it in a spec/design document.

Classification: **systemic pattern** — any agent writing any design/spec doc in this repo can fabricate a plausible identifier. Recurrence test: cross-task YES (three independent expansion agents all did it); cross-project YES; frequency YES (pruning docs show the same failure class at proposal stage); actionability YES (contrastive: assert-from-summary vs grep-then-assert); token justification YES (each occurrence here triggered an essential gate). → **CREATE RULE** (`.claude/rules/grep-verify-codebase-claims.md`, created with this report).

All other issues (missing data-flow section in B; missing banner-deletion sentence in C; missing unparseable fallbacks) are task-specific contract omissions caught by the checklist itself — no rule created (the eval checklist already enforces them; a rule would duplicate it).

## Stage 8: Self-Verification

| # | Question | Answer | Adjustment |
|---|---|---|---|
| 1 | Evidence completeness — did I examine all relevant files? | Read all three solutions, the full eval spec, context, and selection docs completely; grep-verified every codebase-anchored claim I could identify (TYPED_FORCE_SUMMARY, PanelState/jc_layout_v1, GpuForceOptions fields, for_n_nodes, SeedMode, /compute/engines, --compute-url, ?config= path, caffeine-uff.yaml, LayoutPreset/detect, schemars in lock, deny_unknown_fields norm, sdf.toml). C's "A2 favored by judges' merge notes" claim was checked against pruning docs — A2 was a hold/fallback, so the phrase is mild rhetorical inflation, recorded as a Low issue, not scored. | None |
| 2 | Bias check — am I rewarding fluency/length? | B is the shortest document and was not penalized for length — its deductions (CK-205, CK-210, CK-211, UX-3) each cite a quoted structural absence, not brevity. A's confident tone did not prevent its CK-207 gate. | None |
| 3 | Anchor fidelity — both sides quoted, before numbers, on-axis? | Every dimension above carries closer_to AND further_from, each with quoted anchor text and quoted artifact text (or an explicit "artifact lacks: …"), written before the score; numbers were derived from the placement after writing the comparison. All placements stayed on the dimension's contrast axis. | Initial draft had B's Provenance closer_to on score_4; re-reading the axis (absence *explained per-parameter*), B's aggregate UFF chip + silent spring_len absence is structurally the score_2 pattern — corrected closer_to to score_2 with lean toward score_4, score 3 |
| 4 | Comparison integrity — is my reference result itself correct? | The reference's codebase facts were all grep-verified (Stage 2 items 5). The tuple shape of TYPED_FORCE_SUMMARY — the fact driving three CK-207 NOs — is confirmed at graph_canvas.rs:206-207 and :486, and matches the ground-truth doc's own description. | None |
| 5 | Proportionality — three caps at 2.00: me or the spec? | The spec's mechanics: CheckEval strictness ("even minor inaccuracies exclude YES") + the essential gate. Each gate failure is one quoted, grep-verified contradiction, applied uniformly — the same failure class the pruning judges gated 15–16 proposals on. Pre-cap scores (3.52–3.88) are preserved so the uncapped ordering is visible. No thumb on the scale in either direction. | None |

## Strengths

1. **A (C2): applicability as data, enforced identically on every surface** — the manifest predicate model makes dead knobs unconstructable rather than merely hidden (§1 row kinds; §7 "Dead knobs cannot appear here either"), and the override-capsule + parked-override lifecycle is the only design that governs the expert exception truthfully.
2. **C (C1): the strongest data-flow and migration story** — the only document whose pipeline names every stage (`registry load → 5-field record → resolver → regime.options ∘ user_overrides → jc_layout_v2`), the only one that designs `?config=` convergence instead of ignoring it, and the only one that turns presets into deployable data (verified `deny_unknown_fields` norm, verified LayoutPreset/detect deletion target).
3. **B (C3): the calmest defect-class surface and the sharpest anti-proliferation discipline** — the Molecular form (§2.2) is the best default view in the cohort for the measured-defect case, and the cap-5/three-sources gate (§3.1) is the only fully codified seventh-class governance; hybrid sub-line ("scales UFF rests + 13 untyped edges") is the cohort's best partial-coverage truth-text.
4. **All three: judge conditions honored** — CK-206 is YES on every document; the pruning-gate omission (remote/static) is fixed twice over in C and concretely in A and B; no document triggers any pitfall (CK-215–219 all NO on all three).
5. **All three: merge-literate** — every document has a real Mergeability section with shared vocabulary (why-capsule, predicate record, multiplier kind), confirming the judges' C1+C2 one-pattern observation from three independent directions.

## Issues

1. **Priority: High | A/B misname the TYPED_FORCE_SUMMARY shape** | Evidence: A §3 "`typed_coverage = TYPED_FORCE_SUMMARY.bonds_typed / bonds`"; B §1 same expression; real shape `Option<(usize,usize)>` = (typed_nodes, typed_edges), `graph_canvas.rs:206` | Impact: essential gate CK-207 on both; the coverage derivation is not implementable as written | Suggestion: derive coverage as typed_edges (tuple .1) ÷ snapshot edge count and write it that way.
2. **Priority: High | C's flagship regime YAML contains an invalid option** | Evidence: solution.c.md §1.1 `seed: "authored"`; real field `seed_mode` with variants Random/TopoFisheye/None (`gpu_force.rs:96-143`); replaced file uses `seed_mode: none` (`caffeine-uff.yaml:22`) | Impact: CK-207 gate; contradicts C's own "options must deserialize against the engine's struct" rule; lenient parsing would silently map to Random and re-seed over authored positions | Suggestion: `seed_mode: none`; add a boot-time registry self-test.
3. **Priority: High | B omits the contract-required data-flow section** | Evidence: no data-flow section exists in solution.b.md (sections are detection/forms/cross-cutting/walkthroughs/phasing/judge/merge); run-state lines and remote fetch lifecycle have no named signal source | Impact: CK-205 essential gate; row-level rendering is ungradeable and unimplementable without guessing | Suggestion: add the section as a per-form row→signal table.
4. **Priority: Medium | B fabricates a provenance citation** | Evidence: §2.2 "`sdf.toml seed = "authored"`" — no such key in `charts/jump-cannon/packages/sdf.toml` | Impact: false provenance in the exact UI element whose job is truthful provenance; contributes to CK-207 | Suggestion: cite `seed_mode: none` (panel settings) as the authored-position mechanism.
5. **Priority: Medium | No document defines the unparseable/missing-payload fallback** | Evidence: A §5, B §3.3, C §5 all specify v1 handling rules but none states the fallback; today it is `unwrap_or_default` (layout.rs:96-98) | Impact: CK-209 NO on all three; silent reset risk on corrupted localStorage | Suggestion: adopt C's resolution as the universal fallback (re-resolve from the 5-field snapshot record; A's `for_n_nodes` default otherwise).
6. **Priority: Medium | C never deletes the false banner it claims to kill** | Evidence: increment 1 "Kills the three measured lies" + "Today's sliders otherwise unchanged"; the banner (layout.rs:3606-3609) is part of today's panel and is mentioned nowhere in C | Impact: the measured lie could survive C's increment 1; Provenance scored 3 | Suggestion: one sentence — banner deleted in inc 1, superseded by regime reason line + capsules.
7. **Priority: Low | C's empty-match resolution is undefined** | Evidence: §1.2 resolver loop has no stated else-branch; a remote engine with no registry entry is unaddressed | Impact: seventh-case story incomplete (CK-215 answered NO on the strength of vault catch-alls, but the gap is real) | Suggestion: define fall-through to least-specific regime and a generic `engine_kind: remote` entry independent of a named engine.
8. **Priority: Low | C's `spring_len: null` ignores the authored-rescale anchor consumer** | Evidence: layout.rs:110-114 — graph load reads persisted `spring_len` to anchor the authored-coordinate rescale; C's molecular regime nulls it without addressing that reader [severity INFERENCE] | Impact: potential regression of authored-depiction anchoring in the merged design | Suggestion: define the anchor source for data-owned regimes (e.g. UFF mean rest) before nulling.
9. **Priority: Low | B keeps the dense form for the default use case** | Evidence: §2.1 "Essentially today's panel" — ~11 elements before acting on a vault graph | Impact: Operate-Mode scored 3; the doctrine debt persists on the most frequent surface | Suggestion: merge A/C's intent-multiplier default into the Vault form, demoting cooling/backend/halt rows to Advanced.

## Rules Generated

- `.claude/rules/grep-verify-codebase-claims.md` — every codebase identifier asserted in a spec/design/document must be grep-verified against its definition before the document is finalized (root cause of all three CK-207 gate failures; see Stage 7 Five Whys).

## Confidence

- **level: High**
- factors:
  - evidence_strength: Strong — every gate decision and every dimension placement rests on quoted artifact text plus grep-verified codebase facts
  - criterion_clarity: Clear — anchors and checklist questions applied as written; the one judgment call (B CK-205) is documented with its evidence
  - specification_quality: Complete — no missing sections, weights sum to 1.0, gates/tie-breaks/labels all defined
