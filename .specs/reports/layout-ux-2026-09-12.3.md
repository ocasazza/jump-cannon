# Evaluation Report — Layout Panel UI/UX Expanded Solutions (Judge 3)

**Ranking: 1. solution.a (3.88, select) — 2. solution.c (3.45, hold) — 3. solution.b (2.00, reject, essential-gate capped)**

## Metadata
- user_prompt: Design a UI/UX pattern for Jump Cannon's Layout panel fitting all six use cases, replacing the flat-physics-constants panel (Rust+Dioxus only, app.css + panel_kit::CSS, 340–480px dockable panel, Operate-mode doctrine, packages-not-crates, no engine changes for the core pattern).
- artifacts: solution.a.md (C2 capability manifests), solution.b.md (C3 per-graph-class forms), solution.c.md (C1 regime-as-data registry)
- specification_source: .specs/research/layout-ux-2026-09-12.eval-spec.yaml (applied mechanically)
- ground_truth: .specs/research/layout-ux-context.md, .specs/research/layout-ux-2026-09-12.selection.md

## Codebase verification (per spec verification_duty)

| Claim | Result |
|---|---|
| `TYPED_FORCE_SUMMARY` holds `(typed_nodes, typed_edges)` | CONFIRMED — graph_canvas.rs:206, set :279-282, :486. (A and B write `.bonds_typed / bonds`; the signal is a tuple with no such fields — pseudo-naming, concept consistent.) |
| `PanelState{active, settings, seed_strategy, seed_custom}` at `jc_layout_v1` | CONFIRMED — layout.rs:76-83, :53 |
| `GpuForceOptions` fields, `for_n_nodes` cube-root tuning | CONFIRMED — gpu_force.rs:173, :334, :400-420 |
| Remote engines via `/compute/engines` with settings JSON | CONFIRMED — layout.rs:131-133, :420 |
| `?config=` boot presets, `caffeine-uff.yaml` pins spring_len 1.4 | CONFIRMED — app/configs/caffeine-uff.yaml |
| `LayoutPreset::{Fast,Balanced,Pretty}` + detect fingerprint heuristic (C's claim) | CONFIRMED — layout.rs:1079-1147, :3584 |
| False banner "sliders below scale on top of them" | CONFIRMED — layout.rs:3606-3609 |
| schemars "already in the dependency tree" (A's CK-005 claim) | CONFIRMED — app/Cargo.lock (three versions present) |
| **Typed rest semantics** | **Typed rests win outright**: per-edge rest = typed metadata if valid, else `spring_len` fallback (gpu_force.rs:1350-1351; test doc :3542). **No global scale factor on typed rests exists.** Global `repulsion` mixes multiplicatively with per-node weights (`repulsion × √(wᵢ×wⱼ)`, :1233) — honest without engine change. |

**Decisive consequence:** any control specified as "multiplies/scales UFF-typed rests" is not implementable by writing `GpuForceOptions`; at full typed coverage it is a dead knob. All three documents contain this claim, at very different severity:
- **A**: one manifest `note` clause ("scales data-owned rests proportionally when partial", §2) plus the collapsed-row override path — non-load-bearing side claim; the molecular default view (Spread/Stiffness/Weight multipliers, all honest) stands without it.
- **B**: **load-bearing** — "Geometry scale … multiplies every UFF rest proportionally … the control that *would have* answered 'why is my molecule tiny' honestly" (§2.2) and the user-facing sub-line "scales UFF rests + 13 untyped edges" (§3.2). B's flagship form's centerpiece contradicts documented engine behavior; B never justifies an engine change (Increment 1 is "~150-line form"). Answered CK-207 NO accordingly.
- **C**: `applies_to: [spring_len, typed_rests]` on `geometry_scale` in the flagship regime YAML (§1.1) — a real defect, but C's score rests on the registry/resolver/migration architecture, which stands if that one data line is corrected; penalized in the Control Honesty dimension instead of the essential gate.

---

## solution.a.md — C2 Engine Capability Manifests

### Checklist
```yaml
- id: CK-201
  answer: YES
  evidence: "§4 walks all six cases with panel state (1: 'Vault · large · why ▸', Backend enum; 2: '25/25 rests from UFF' capsule, banner 'gone'; 5: cached manifest 'stale' + backoff; 6: 'execution: one_shot' → Solve + quality); cases 3-4 thin but concrete-by-equivalence ('Same as vault-small; regime Generated')."
- id: CK-202
  answer: YES
  evidence: "§1 row kinds: 'Absent rows — not rendered at all'; Advanced is 'manifest-filtered … Dead knobs cannot appear here either (CK-202)'; data-owned dims are collapsed capsules requiring explicit 'custom'-marked override."
- id: CK-203
  answer: YES
  evidence: "§2: JSON manifests via include_str!, schemars derive (verified in Cargo.lock), build-time generator — all Rust; no JS or external styling anywhere in the document."
- id: CK-204
  answer: YES
  evidence: "§6: core pattern (manifest + rendering) needs no engine change; schemars derives + #[capability] attributes quarantined in Increment 3 with CK-005 justification (§7)."
- id: CK-205
  answer: YES
  evidence: "§1 IA+wireframes, §2 manifest, §3 data flow (named signals), §4 walkthroughs, §5 persistence/migration, §6 kaizen, §7 judge responses — all contract sections present."
- id: CK-206
  answer: YES
  evidence: "§7: CK-006 → why-capsule adopted ('C1's regime capsule adopted as C2's provenance display', §1); CK-005 → 'manifests are projections of serde schemas + colocated capability attributes … drift is a compile error' (§2.1)."
- id: CK-207
  answer: YES
  evidence: "All enumerated integration points verified consistent (table above); schemars claim verified. Caveat: the §2 note 'scales data-owned rests proportionally' contradicts engine semantics (typed rests win outright, gpu_force.rs:1350-1351) — non-load-bearing, penalized in Control Honesty/Provenance dimensions."
- id: CK-208
  answer: YES
  evidence: "§1 wireframes are ~40-column single-panel rows; 'Advanced ▸' is a collapsed drawer inside the same panel; no modal/takeover surface."
- id: CK-209
  answer: NO
  evidence: "§5 gives an explicit rule ('legacy absolutes ÷ vault base → vault-regime overrides') but defines no fallback for missing/unparseable state and never names a versioned writer key/schema."
- id: CK-210
  answer: YES
  evidence: "§3/§5: persists '{engine, regime_id, overrides}' multiplier-keyed; 'never absolutes for multiplier dims, so vault values can't leak onto ångström geometry'; override lifecycle with parked-override surfacing."
- id: CK-211
  answer: YES
  evidence: "§2: manifests are JSON data; §2.1: 'No hand-written per-engine tables' — projection of serde schemas, drift is a compile error; no compiled per-engine UI."
- id: CK-212
  answer: YES
  evidence: "§6 Increment 1: 'manifest for gpu-force only, hand-checked … spring_len capsule + backend absence + why-capsule. Kills the two measured lies. No other UI change.'"
- id: CK-213
  answer: YES
  evidence: "Whole document develops capability manifests; §8 maps other proposals as renderings/merges of the manifest mechanism — no bait-and-switch."
- id: CK-214
  answer: YES
  evidence: "§2: 'Controls default to dimensionless multipliers around a resolved base (adopting B1's write-through semantics …)'; §6 Increment 2 lands them."
- id: CK-215
  answer: NO
  evidence: "§2.1: remote engines without manifest get the generic fallback form; new local engines derive manifests from schemas — seventh-case behavior defined, one closed predicate set."
- id: CK-216
  answer: NO
  evidence: "Resolution is always narrated: why-capsule shows 'base molecular-uff (auto: 25/25 bonds UFF-typed)' plus per-row owned_by capsules (§1)."
- id: CK-217
  answer: NO
  evidence: "§1 why-capsule: 'presets fast/balanced/pretty hidden (vault-tuned)' for the molecular regime."
- id: CK-218
  answer: NO
  evidence: "§1: Advanced is 'the manifest-filtered complete form … each either a live control or a collapsed-data capsule' — no unfiltered GpuForceOptions sliders or raw JSON editor anywhere."
- id: CK-219
  answer: NO
  evidence: "Thin cases are specified by equivalence ('Same as vault-small', case 3) with concrete deltas named; data flow (§3) names every signal — no 'adapts appropriately' hedges found."
```

### Rubric dimensions

1. **Six-Use-Case Coverage Depth** (0.16) — **score 4**
   Closer to score_4 ("controls render from the engine's existing /compute/engines settings JSON … falls back to the same generic renderer"): A's §4.5 renders from the served manifest with a 'stale' cached-manifest path, and §2.1 defines the manifest-less fallback ("settings as declared by <engine> — applicability unknown"). Further from score_2 ("Remote engines and static solvers keep their existing forms for now"): nothing is deferred — case 6 gets a one_shot manifest with Solve + quality and no live rows. Cases 3-4 are one-clause walkthroughs (equivalence to vault-small plus named deltas), which keeps this at a match rather than beyond the anchor.
2. **Control Honesty and Applicability** (0.18) — **score 4**
   Closer to score_4 ("advanced drawer emits only rows whose applicability predicate is live … spring_len row is absent here too"): A's Advanced is "manifest-filtered … Dead knobs cannot appear here either" (§1). Further from score_2 ("Advanced reveals the raw GpuForceOptions sliders"): no raw surface exists in the document. Dent: the §2 note claiming spring_len "scales data-owned rests" contradicts engine behavior, and the collapsed-row override at full coverage would open a dead custom knob — honesty enforced structurally on all surfaces, but the override semantics and note overclaim prevent exceeding the anchor.
3. **Provenance Legibility** (0.12) — **score 4**
   Closer to score_4 ("why-capsule reads '25 bond lengths from UFF import — spring length is data-owned'; the old banner is deleted"): A ships exactly this ("▸ 25/25 rests from UFF"; walkthrough 2: "the false 'sliders scale on top' banner is gone — replaced by per-row truth") plus a full why table (base/rests/weights/presets/yours) and parked-override surfacing. Further from score_2 ("controls simply not rendered; banner reads 'Molecular parameters active'"): the banner is explicitly deleted. Not beyond the anchor: the manifest's own `note` field carries a false scaling claim, i.e. one provenance-bearing text lies.
4. **Operate-Mode UX Quality** (0.12) — **score 4**
   Closer to score_4 ("regime capsule plus an intent row; raw rows in a collapsed drawer"): the molecular default view is capsule + 3 multipliers + toggle + collapsed Advanced; the vault default prunes non-applicable rows (backend absent <500 nodes) and compresses to multipliers with a caution chip on gravity. Further from score_2 ("a 'Molecular mode' checkbox above the existing ten sliders"): no mode checkbox, no preserved dense form. Slightly more rows than the anchor's minimal intent row keeps it at a match.
5. **Constraint Feasibility and Data-Flow Specificity** (0.12) — **score 4**
   Closer to score_4 ("row emission keys off TYPED_FORCE_SUMMARY plus node count; remote controls from /compute/engines settings JSON"): §3 names TYPED_FORCE_SUMMARY, for_n_nodes, source kind, snapshot-rev memo, jc_layout_v1, /compute/engines; schemars verified present. Further from score_2 ("reads the current graph state and renders appropriate controls"): every render decision is signal-named. Minor slip: `.bonds_typed / bonds` pseudo-fields vs the actual `(typed_nodes, typed_edges)` tuple.
6. **Migration and Persistence Soundness** (0.12) — **score 3**
   Past score_2 ("bump the key; old settings discarded"): A reinterprets v1 absolutes as multipliers against the vault base — lossless-tolerant rule, ?config path explicitly kept. Short of score_4 ("missing regime field → resolve; unparseable → for_n_nodes fallback; writer emits v2 with schema: 2"): artifact lacks an unparseable/missing fallback and never names the writer key or schema version; the "migrates … once" + "?config untouched" pair leaves the re-boot-rewrites-v1 interaction undefined.
7. **Kaizen Phasing** (0.08) — **score 4**
   Closer to score_4 ("Phase 1 ships alone: truthful banner + applicability-filtered rows — the lying spring_len slider is deleted"): Increment 1 is one engine's hand-checked manifest + capsule + absence + why-capsule, "Kills the two measured lies. No other UI change." Further from score_2 ("Phase 1: schema + renderer + migration + presets"): migration is Increment 2, generation Increment 3, remote Increment 4.
8. **Mergeability and Synthesis** (0.10) — **score 4**
   Closer to score_4 ("regime capsule and capability manifest are one applicability record; B1 adopted as multipliers"): §8 — "C1's regimes/*.yaml becomes the source of the regimes array + the why-capsule content … C3's forms are simply the renderings this manifest produces"; B1 adopted in §2. Further from score_2 ("never mentions the other proposals"): all three named with concrete merge surfaces.

### Score calculation
- Raw weighted sum: 0.64+0.72+0.48+0.48+0.48+0.36+0.32+0.40 = **3.88**
- Essential gate: all YES → no cap. Pitfalls: none YES → no penalty.
- **Final: 3.88**

---

## solution.b.md — C3 Per-Graph-Class Forms

### Checklist
```yaml
- id: CK-201
  answer: YES
  evidence: "§2.1-2.4 give full wireframed forms for Vault (cases 1,3,4), Molecular (case 2), Remote (case 5), Static (case 6); §4 maps each case to its form with concrete deltas ('n=144 → backend row hidden')."
- id: CK-202
  answer: YES
  evidence: "§2.2: 'No global spring_len knob exists anywhere in this form'; §2.1 vault knobs all genuinely live for untyped graphs; Advanced is a resolved-UFF table, not raw options."
- id: CK-203
  answer: YES
  evidence: "§3.1: forms are '~40-line compositions' of a Rust component kit (slider_row, chip, why_capsule); no JS or external styling anywhere in the document."
- id: CK-204
  answer: YES
  evidence: "No engine change is requested anywhere (§5 increments are UI/data only) — though see CK-207: the flagship control silently assumes engine behavior that does not exist."
- id: CK-205
  answer: YES
  evidence: "IA §1-2 (four exact forms), data flow via §1 predicate table over named signals (GraphSnapshot, TYPED_FORCE_SUMMARY, engine kind, source kind) + §3.3 persistence keys, walkthroughs §4, persistence/migration §3.3, kaizen §5, judge responses §6, four wireframes."
- id: CK-206
  answer: YES
  evidence: "§3.1: 'Form count is hard-capped at 5 … three distinct sources' seventh-class gate; §3.2: ≥0.5 coverage → Molecular + hybrid section, <0.5 → Vault + 'uff partial' chip."
- id: CK-207
  answer: NO
  evidence: "§2.2 'Geometry scale — multiplies every UFF rest proportionally' and §3.2 'scales UFF rests + 13 untyped edges' contradict verified engine behavior: typed rests win outright, spring_len is only the untyped/invalid fallback (gpu_force.rs:1350-1351, :3542); no global rest-scale factor exists. The document's high score rests on this claim ('the control that would have answered why is my molecule tiny honestly'; §4.2 'the working answer')."
- id: CK-208
  answer: YES
  evidence: "All four form wireframes are ~40-column single-panel compositions; Advanced is an in-panel disclosure."
- id: CK-209
  answer: NO
  evidence: "§3.3: 'jc_layout_v1 migrates into .vault verbatim' — versioned per-class keys and an explicit rule, but no fallback for missing/unparseable state is defined."
- id: CK-210
  answer: YES
  evidence: "§3.3: per-class keys 'so switching class never clobbers the other class's tuning'; §2.2/§3.2 sub-lines state what each multiplier governs, fixing L0/L1/L2 collision structurally per class."
- id: CK-211
  answer: NO
  evidence: "§2: the four forms are hand-composed compiled Rust ('~40-line compositions' per form) with compiled detection predicates — per-class behavior lives in code, not configuration; only the Remote form (settings-JSON rendering) is data-driven."
- id: CK-212
  answer: YES
  evidence: "§5 Increment 1: 'Molecular form only … Kills all three lies with one new ~150-line form', other classes keep today's panel."
- id: CK-213
  answer: YES
  evidence: "The document is per-graph-class forms throughout ('The pattern is the class switch plus shared components, not one adaptive mega-form')."
- id: CK-214
  answer: YES
  evidence: "§2.2: Geometry scale is 'B1's Spread, specialized'; §7: 'B1's intent multipliers appear as Geometry scale / Repulsion (atoms)'."
- id: CK-215
  answer: NO
  evidence: "§3.1: hard cap at 5 forms, codified three-sources gate, seventh class uses the Vault default."
- id: CK-216
  answer: NO
  evidence: "§1: detection result shown as title chip ('Molecular ▾') with manual override menu and 'auto'."
- id: CK-217
  answer: NO
  evidence: "§2.2 why▸: 'vault presets hidden (would clobber UFF rests)'; the Molecular form has no presets row."
- id: CK-218
  answer: NO
  evidence: "§2.2 Advanced is a 'read-mostly table of resolved UFF parameters … with per-row override toggles' — no unfiltered GpuForceOptions sliders; Remote form renders only the engine's own declared schema under an honesty header."
- id: CK-219
  answer: NO
  evidence: "All walkthroughs resolve to fully wireframed forms with named deltas; no 'adapts appropriately' hedges found."
```

### Rubric dimensions

1. **Six-Use-Case Coverage Depth** (0.16) — **score 4**
   Closer to score_4 ("controls render from the engine's existing /compute/engines settings JSON"): §2.3 does exactly this with type-inferred controls and the honesty header, plus an upward path when a manifest is served; seventh-class behavior is the most codified of the three (three-sources gate, cap 5, Vault default, §3.1) and hybrid coverage gets its own wireframe (§3.2). Further from score_2 ("Remote engines and static solvers keep their existing forms for now"): both get purpose-built forms. Not beyond the anchor: the score_4 anchor's schema-less fallback ("generic renderer over {}") and broker-down behavior are never specified.
2. **Control Honesty and Applicability** (0.18) — **score 2**
   Closer to score_2 (a data-owned/wrong-regime knob still reachable): the Molecular form's default view renders "Geometry scale" live at full 25/25 UFF coverage where it is a dead knob — typed rests win outright (gpu_force.rs:1350-1351) and no global rest-scale factor exists — while its sub-line falsely claims it "scales UFF rests + 13 untyped edges" (§3.2). This reincarnates the measured dead-spring_len lie as the flagship form's centerpiece. Further from score_4 ("advanced drawer emits only live-applicability rows"): the escape surfaces are clean ("No global spring_len knob exists anywhere in this form", §2.2) and Remote/Vault forms are honest, but the dimension audits the default view too, and there the central control lies.
3. **Provenance Legibility** (0.12) — **score 3**
   Matches score_4 in mechanism: why▸ expansion (element list, bond-order histogram, authored-coordinate source, "vault presets hidden"), `n-tuned` tag on spring_len, banner explicitly removed ("the misleading banner absent", §2.1). But one displayed provenance line is false — "scales UFF rests + 13 untyped edges" tells the user a multiplier governs data-owned rests it cannot touch — and provenance that misstates ownership is the failure this dimension polices. Past score_2 (capsule exists, banner deleted), short of score_4 (truthful per-parameter text not achieved on the hybrid path).
4. **Operate-Mode UX Quality** (0.12) — **score 3**
   The molecular/static/remote defaults are genuinely calm (2 sliders + toggle; Solve + quality; declared settings). But the vault default — the most common case — is "Essentially today's panel, minus the lies" (§2.1): presets row, four absolute sliders, cooling row, backend/seed row, halt line. Past score_2 (no added mode toggle; the lies are removed), short of score_4 (the vault default remains the dense flat form rather than capsule + intent + drawer).
5. **Constraint Feasibility and Data-Flow Specificity** (0.12) — **score 3**
   Past score_2: detection keys off named sources (GraphSnapshot, TYPED_FORCE_SUMMARY ratio, engine kind, source kind; §1) and Remote rows render from the engine's settings JSON (§2.3). Short of score_4: no consolidated data flow; the edit write-path is never specified (how a slider edit reaches GpuForceOptions is only implied via the B1 reference in §7), and the Geometry-scale mechanism is infeasible as specified (no engine option scales typed rests).
6. **Migration and Persistence Soundness** (0.12) — **score 3**
   Past score_2: versioned per-class keys (`jc_layout_v2.vault`, `.molecular`), v1 → `.vault` verbatim rule, `form_override: Option<LayoutForm>` with None = auto-resolve (maps the anchor's "missing field → resolve"), and the cross-class leak is named and fixed (§3.3). Short of score_4: no unparseable fallback, and the ?config= boot-preset interaction (caffeine-uff.yaml) is never examined — the dimension instruction names this as a discount.
7. **Kaizen Phasing** (0.08) — **score 4**
   Closer to score_4: Increment 1 ships the Molecular form + detection/override alone ("Kills all three lies with one new ~150-line form"); migration, chips, Remote, Static each land as separate justified increments. Further from score_2: no bundling of schema+migration+presets in phase 1.
8. **Mergeability and Synthesis** (0.10) — **score 4**
   Closer to score_4: §7 — "C3 is the design target (what each class should see), C2 the engine for getting there"; class predicates = C1's regime-resolution predicates; Molecular why▸ = C1's capsule; B1 adopted as named controls. Further from score_2: all three proposals addressed with concrete shared vocabulary.

### Score calculation
- Raw weighted sum: 0.64+0.36+0.36+0.36+0.36+0.36+0.32+0.40 = **3.16**
- Essential gate: **CK-207 = NO → capped at 2.0** (spec essential_gate; gate source: specification). Pitfalls: none YES.
- **Final: 2.00**

---

## solution.c.md — C1 Regime-as-Data Registry

### Checklist
```yaml
- id: CK-201
  answer: YES
  evidence: "§4 walks all six cases with resolution + rendered state (1: vault-large; 2: molecular-uff with ?config convergence; 5: 'settings_schema: broker' generic rendering + stale cache; 6: 'execution: one_shot' → Solve + quality); remote and static additionally get worked YAML (§1.1)."
- id: CK-202
  answer: YES
  evidence: "§2 Band 3: 'Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere (CK-202)'; data-owned dims (option null + typed coverage) are collapsed capsules on every surface."
- id: CK-203
  answer: YES
  evidence: "Entire mechanism is YAML + serde + a resolver in Rust (§1.1 'serde(deny_unknown_fields)'); no JS or external styling anywhere in the document."
- id: CK-204
  answer: YES
  evidence: "§1.1: 'options must deserialize against the named engine's options struct' — validation against the engine as-is; no engine change requested in any increment (§6). Caveat: geometry_scale's typed_rests claim would need one — defect scored in Control Honesty, not requested as engine work."
- id: CK-205
  answer: YES
  evidence: "§1 registry/IA, §2 panel + wireframe, §3 data flow (named), §4 walkthroughs, §5 persistence/migration, §6 kaizen, §7 judge responses — all contract sections present."
- id: CK-206
  answer: YES
  evidence: "§7: 'Pruning gate (remote/static never named): both are first-class registry citizens with worked YAML (§1.1) and walkthroughs (§4.5–6)'; ray-force-3d.yaml and force-atlas-static.yaml are fully worked."
- id: CK-207
  answer: YES
  evidence: "All enumerated integration points verified consistent, including the LayoutPreset detect-fingerprint claim (layout.rs:1144) and caffeine-uff.yaml shape. Caveat: geometry_scale 'applies_to: [spring_len, typed_rests]' contradicts engine semantics (typed rests win outright) — non-load-bearing for the architecture; penalized in Control Honesty."
- id: CK-208
  answer: YES
  evidence: "§2 three-band wireframe is a ~40-column single panel; Advanced is an in-panel disclosure."
- id: CK-209
  answer: NO
  evidence: "§5: 'Legacy jc_layout_v1 absolutes migrate into custom regime overrides against vault-large' — versioned key + explicit rule, but no fallback for missing/unparseable payloads is defined."
- id: CK-210
  answer: YES
  evidence: "§3: 'effective options = regime.options ∘ user_overrides; null + data-owned dims filled from TYPED_FORCE_SUMMARY'; 'overrides stored by control id against the regime base' — explicit L0/L1/L2 composition order."
- id: CK-211
  answer: YES
  evidence: "§3: 'presets become deployable data, honoring packages-not-crates'; §5: presets and caffeine-uff.yaml migrate to YAML, LayoutPreset enum deleted; behavior is registry data, not compiled tables."
- id: CK-212
  answer: YES
  evidence: "§6 Increment 1: registry schema + loader + molecular-uff.yaml + one-predicate resolver + capsule/collapsed rows; 'Kills the three measured lies'; migration, presets, controls, remote each deferred to later increments."
- id: CK-213
  answer: YES
  evidence: "The document is the regime-as-data registry throughout; §1 explicitly 'generalizing caffeine-uff.yaml from one-off boot preset to a kind of thing'."
- id: CK-214
  answer: YES
  evidence: "§8: 'B1's intent multipliers are the kind: multiplier controls'; §1.1 controls use kind: multiplier with declared ranges."
- id: CK-215
  answer: NO
  evidence: "§1.1: generic 'engine_kind: remote' / 'engine_kind: static' entries absorb any new engine of those kinds; new regimes are data files, not compiled forms — seventh-case behavior defined by predicate generality."
- id: CK-216
  answer: NO
  evidence: "§1.2: 'The resolution result is always shown with its reason: Molecular · UFF (auto: 25/25 bonds UFF-typed)'."
- id: CK-217
  answer: NO
  evidence: "§1.1: 'presets_hidden: [fast, balanced, pretty] # vault-tuned; quarantined for this regime' in molecular-uff.yaml."
- id: CK-218
  answer: NO
  evidence: "§2: 'Advanced shows the regime's full option set, data-owned entries as capsules' — filtered by the regime schema; no unscoped raw GpuForceOptions or raw-JSON surface anywhere."
- id: CK-219
  answer: YES
  evidence: "Walkthrough 3 hedges: 'resolves generated … → vault-small-like options + a generated note' — generated.yaml is listed (§1) but never worked, and 'vault-small-like' leaves the options unspecified; the resolver (§1.2) also never states the no-match fallback behavior."
```

### Rubric dimensions

1. **Six-Use-Case Coverage Depth** (0.16) — **score 4**
   Closer to score_4 ("controls render from the engine's existing /compute/engines settings JSON"): walkthrough 5 does exactly this under an honesty header with stale-cache and backoff behavior, and remote/static are worked registry YAML — the precise fix for the pruning gate. Further from score_2 ("keep their existing forms for now"): nothing deferred. Held at a match: the generated regime's contents are hedged ("vault-small-like options"), the schema-less-broker fallback is unstated, and the resolver's no-match behavior is undefined.
2. **Control Honesty and Applicability** (0.18) — **score 3**
   On the anchor axis (escape surfaces), C is clean: "dead knobs unrepresentable everywhere" via null→capsule representation (§2), presets quarantined per regime. But the dimension also audits the default view, and there the flagship molecular wireframe renders "Geometry scale" (§2) whose declared `applies_to: [spring_len, typed_rests]` (§1.1) is impossible — at 25/25 coverage writing spring_len is dead (typed rests win outright, gpu_force.rs:1350-1351) and no option scales typed rests. A dead/mis-scaled knob on the flagship default view pulls the placement below the score_4 match; the otherwise-representational honesty (null in data ⇒ capsule, structurally unrepresentable dead knobs) keeps it past score_2.
3. **Provenance Legibility** (0.12) — **score 4**
   Closer to score_4 ("why-capsule … 'spring length is data-owned'; old banner deleted, not reworded"): the regime line always carries its resolution reason ("auto: 25/25 bonds UFF-typed", §1.2), the data-owned capsule names its source ("25/25 rests from UFF", §2), overrides are tracked against the regime base so a YAML edit's effect is visible (§3), and Increment 1 "kills the three measured lies" (banner among them, §6). Further from score_2 ("controls simply not rendered; banner reads 'Molecular parameters active'"): absence is always explained.
4. **Operate-Mode UX Quality** (0.12) — **score 3**
   The molecular default (§2) is calm: capsule + 2 multipliers + toggle + settled line + collapsed Advanced. But the vault default "reproduce[s] today's behavior exactly — today's panel is the special case where the regime declares the familiar absolute sliders" (§2), so the most common case keeps the dense flat form. Past score_2 (dense form, but no added lying toggle and with a regime capsule), short of score_4 (no capsule-plus-intent default for vault).
5. **Constraint Feasibility and Data-Flow Specificity** (0.12) — **score 4**
   Closer to score_4 ("keys off TYPED_FORCE_SUMMARY plus node count; remote from /compute/engines settings JSON"): §3 names the whole pipeline — boot-time registry load with deny_unknown_fields validation against the engine's serde struct, the five-field snapshot state record, resolver, options composition, jc_layout_v2 persistence — and §5 converges the ?config loader onto the same schema. Further from score_2 ("reads the current graph state"): no unnamed "state" anywhere. Held at match by the "vault-small-like" hedge and unstated no-match fallback.
6. **Migration and Persistence Soundness** (0.12) — **score 4**
   Closer to score_4 ("reader accepts v1 … settings re-keyed … writer emits v2"): jc_layout_v2 named with shape (§5), v1 absolutes → custom overrides against vault-large (their historic base), resolution covers the missing-field case, and the ?config interaction is the deepest of the three — one loader, one schema, caffeine-uff.yaml becomes a registry entry, "no two formats" (§4.2, §5), with the LayoutPreset enum and its detect fingerprint deleted in a clean cutover. Short of a full match only on the unstated unparseable fallback.
7. **Kaizen Phasing** (0.08) — **score 4**
   Closer to score_4: Increment 1 ships alone (schema + loader + one regime + one-predicate resolver + capsule/collapsed rows, sliders otherwise unchanged) and "kills the three measured lies"; migration (2), controls (3), remote/static (4) are separate justified layers. Further from score_2 ("Phase 1: schema + renderer + migration + presets"): none of migration/presets/controls is bundled into phase 1. Heavier than the anchor's banner-plus-filter phase 1, but irreducible for a registry pattern.
8. **Mergeability and Synthesis** (0.10) — **score 4**
   Closer to score_4 ("the capability manifest is the engine-side half of a regime entry; B1 adopted as multipliers"): §8 maps all three — C2 manifests slot into per-engine controls/applicability, B's forms are "what this registry renders", B1 multipliers are `kind: multiplier` — and names C1's concrete synthesis contributions. Further from score_2: the one-pattern observation is explicitly addressed.

### Score calculation
- Raw weighted sum: 0.64+0.54+0.48+0.36+0.48+0.48+0.32+0.40 = **3.70**
- Essential gate: all YES → no cap. Pitfalls: CK-219 YES → −0.25.
- **Final: 3.45**

---

## Ranking and labels

| Rank | Document | Raw | Adjusted | overall_label |
|---|---|---|---|---|
| 1 | solution.a (C2 manifests) | 3.88 | **3.88** | **select** |
| 2 | solution.c (C1 registry) | 3.70 | **3.45** | **hold** (merge contributor) |
| 3 | solution.b (C3 forms) | 3.16 | **2.00** | **reject** (essential-gate capped: CK-207) |

**rank_justification:** A is the only document whose default view is honest in every walkthrough — its molecular view renders only multipliers that the engine actually honors (repulsion mixes via √(wᵢ×wⱼ); spring_k governs all springs), with applicability enforced identically on the Advanced surface, schemars-derivation verified against Cargo.lock, and the strongest phase-1 story. C matches A architecturally and beats it on migration/persistence (named v2 key, ?config schema convergence, clean LayoutPreset cutover) but ships a hedge in walkthrough 3, an undefined resolver fallback, a dense vault default, and the impossible `typed_rests` scaling declaration in its flagship regime. B has the best seventh-class governance and the clearest forms, but its centerpiece control — Geometry scale "multiplies every UFF rest proportionally" — contradicts verified engine behavior (typed rests win outright; no global rest-scale factor exists), reintroducing the dead-knob lie in the form whose purpose was to kill it; per the spec's verification duty this answers CK-207 NO and caps the document at 2.0. No tie-breakers were needed.

## merge_map (into primary = solution.a)

**From solution.c (C1) — merge:**
1. The YAML regime registry (`app/configs/regimes/*.yaml`) as the data source for A's manifest `regimes` array and the regime picker, with the specificity-sorted first-match resolver over the shared five-field snapshot record.
2. `?config=` schema convergence — one loader, one schema; boot preset = regime referenced by URL (fixes A's undefined "?config re-writes v1 after one-shot migration" interaction).
3. Presets-as-data migration: fast/balanced/pretty → YAML, LayoutPreset enum + detect fingerprint deleted (clean cutover), `presets_hidden` per regime.
4. Override-against-regime-base persistence (`overrides: {control_id: value}` keyed to a named regime) so registry YAML tuning propagates to users without overrides.
5. `execution: live | one_shot` as the declared driver of live-vs-static row sets (A currently buries this in a manifest example).
6. Named writer versioning (`jc_layout_v2`) — A never names its writer key/schema.
- **Reject from C:** `geometry_scale` with `applies_to: typed_rests` (impossible without engine change); the "vault-small-like" generated-regime hedge (work generated.yaml properly or route generated → vault-small explicitly).

**From solution.b (C3) — merge:**
1. The bounded-specialization governance: form/manifest authoring gate (three distinct sources before a bespoke entry; cap) applied to manifest/regime authorship.
2. Hybrid-coverage predicate semantics: ≥0.5 typed coverage → molecular applicability with hybrid rendering; <0.5 → vault with an `uff partial` chip — as concrete `typed_bond_coverage` predicate thresholds in A's closed predicate set.
3. User-facing copy: `n-tuned` provenance tag, `untyped edges — global rest scale applies` chip, `settled ✓ (halt 0.08, 0.4s)` one-line run state for small converged graphs.
4. The Molecular form as the design target A's manifest rendering should reproduce for typed graphs (A §8 already frames C3 forms as "the renderings this manifest produces").
- **Reject from B:** Geometry scale as specified (dead at full typed coverage; requires engine change to scale UFF rests); the compiled per-class form structure itself (superseded by manifest-driven rendering).

**Already in primary:** C1's why-capsule (A §1) and B1's dimensionless multipliers writing through into GpuForceOptions (A §2) are adopted; the repulsion/Weight multiplier is verified honest against the engine's √(wᵢ×wⱼ) mixing.

## Confidence
```yaml
confidence:
  level: High
  factors:
    evidence_strength: Strong — 12 codebase claims grep-verified including the decisive typed-rest engine semantics (gpu_force.rs:1350-1351, :3542, :1233)
    criterion_clarity: Clear — anchors and checklist map cleanly onto all three documents
    specification_quality: Complete — no missing dimensions, checklist items, or scoring metadata
```
