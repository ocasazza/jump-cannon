# Evaluation Report 2 — Layout panel UI/UX expansions (2026-09-12)

Judge: EvalJudge2. Specification: `.specs/research/layout-ux-2026-09-12.eval-spec.yaml` (applied mechanically, unmodified).
Artifacts: `solution.a.md` (C2 capability manifests), `solution.b.md` (C3 per-graph-class forms), `solution.c.md` (C1 regime-as-data registry).
Ground truth: `.specs/research/layout-ux-context.md`, `.specs/research/layout-ux-2026-09-12.selection.md`.

## RANKING

1. **solution.a.md (C2) — 3.88 — SELECT** (primary pattern)
2. **solution.c.md (C1) — 3.68 — HOLD** (merge contributor)
3. **solution.b.md (C3) — 2.00 (gate-capped from 3.48) — REJECT** (essential gate: CK-205 NO — no data-flow section)

---

## Codebase verification (verification_duty)

Grep-verified against the repo before scoring:

| Claim relied on | Result |
|---|---|
| `TYPED_FORCE_SUMMARY` holds `(typed_nodes, typed_edges)` and drives the banner | VERIFIED — `app/ui/src/graph_canvas.rs:206` (`GlobalSignal<Option<(usize,usize)>>`), written at :279/:486 |
| False banner "sliders scale on top" | VERIFIED — `app/ui/src/panels/layout.rs:3606-3609` |
| `PanelState { active, settings, seed_strategy, seed_custom }` at `jc_layout_v1` | VERIFIED — `layout.rs:53,76-83`; `appstate.rs:66,127,153` |
| `GpuForceOptions::for_n_nodes` cube-root scaling | VERIFIED — `crates/graph-layouts/src/layout/algorithms/gpu_force.rs:334` (note: symbol lives in graph-layouts, not `crates/graph-api/src/compute.rs`; substance confirmed) |
| `LayoutPreset::{Fast,Balanced,Pretty}` hardcoded with spring_len 40/60/80 + detect heuristic | VERIFIED — `layout.rs:1079-1131` (40/60/80 confirmed), `layout.rs:3584` `LayoutPreset::detect(&opts)` — solution.c's "enum and its detect-fingerprinting heuristic are deleted" is accurate |
| `?config=` boot presets / caffeine-uff.yaml (spring_len 1.4, repulsion 0.02, grid, gravity 0, seed none) | VERIFIED — `app/configs/caffeine-uff.yaml` matches ground truth exactly |
| schemars "already in the dependency tree" (solution.a §2.1) | VERIFIED — `app/Cargo.lock` lines 4567-4610 (multiple versions, transitive via tauri etc.) |
| Remote engines' `/compute/engines` settings JSON | PARTIAL NUANCE — `/compute/engines` exists (`crates/graph-api/src/server.rs:90`) but `EngineView` = `{id, display_name, description, kind}` only (`compute_broker.rs:609-614`). Per-engine settings exist as `serde_json::Value` (Subscribe `params` / `set_params`, proto `compute.proto:60-64`) and per-engine JSON in PanelState, but **no settings schema is served by the endpoint today**. All three documents are consistent with the ground-truth text ("per-engine settings JSON"); solution.c's `settings_schema: broker` ("options schema fetched from /compute/engines") is the strongest reliance on the not-yet-existing schema surface — phase-4 quarantined, flagged below. Engine `kind` "Physics"/"Static" VERIFIED (`compute.proto:106`, `service.rs:104-107`), grounding all three docs' static-engine detection. |

No document's high score rests on claims contradicting the ground truth. CK-207 answered YES for all three.

---

## Reference result (generated before comparative analysis)

A correct full solution must: (1) walk all six use cases with rendered panel state, present/absent controls, and provenance text; (2) make data-owned dimensions unrepresentable as live knobs on EVERY surface including escape hatches, solving the vault-unit/ångström collision structurally; (3) show per-parameter provenance and explicitly delete the "sliders scale on top" banner; (4) give a named-signal data flow (TYPED_FORCE_SUMMARY, for_n_nodes, PanelState, /compute/engines, ?config=); (5) specify v1 migration with fallback + boot-preset compatibility; (6) phase 1 = smallest lie-killing increment; (7) answer its branch's judge conditions; (8) stay Rust+Dioxus, app.css/panel_kit, no engine changes in core, packages-not-crates; (9) take a concrete B1 position and bound specialization with defined seventh-case behavior.

---

## Document 1: solution.a.md — C2 Engine Capability Manifests

### Checklist

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walks all six: "1. Vault 10.2k… 2. Molecular caffeine… 3. Generated grid… 4. Untyped importer… 5. Remote engine with manifest… 6. Remote engine without manifest + static solver", each with control presence/absence and regime-capsule text |
| CK-202 | YES | §1 row-kind 4: "Advanced — the manifest-filtered complete form… Dead knobs cannot appear here either (CK-202)"; collapsed-data rows "require explicit override to edit" (§7) |
| CK-203 | YES | All mechanisms are Dioxus rsx rows/capsules + build-time Rust generation (§2.1); no JS, no styling pipeline mentioned |
| CK-204 | YES | Core = hand-checked JSON manifest + panel rendering; schemars derives/attributes quarantined to "Increment 3" (§6), remote serving to Increment 4 |
| CK-205 | YES | §1 IA + 2 ASCII wireframes, §3 data flow (named signals), §4 walkthroughs, §5 persistence/migration, §6 kaizen, §7 judge-condition responses |
| CK-206 | YES | §2.1 "Deriving local manifests from serde schemas (CK-005)"; §1/§7 why-capsule adopting C1's provenance (CK-006) — both branch conditions answered |
| CK-207 | YES | TYPED_FORCE_SUMMARY/for_n_nodes/jc_layout_v1/banner all grep-verified; "molecule users keep `?config` boot path untouched" (§5); schemars claim verified in Cargo.lock |
| CK-208 | YES | §1 wireframe is a single narrow column of rows inside `┌ Layout ─┐`, dockable-panel sized |
| CK-209 | NO | §5 gives an explicit rule ("legacy absolutes ÷ vault base → vault-regime overrides") but no version field on the new record (new key never even named) and **no defined fallback for missing/unparseable state** |
| CK-210 | YES | §3/§5: persists `{regime_id, overrides}` multiplier-keyed, "never absolutes for multiplier dims, so vault values can't leak onto ångström geometry"; parked-override lifecycle states data>override precedence |
| CK-211 | YES | Behavior driven by JSON manifest data with derived applicability ("manifest is a projection of the Rust source"); truth colocated, not a new UI table (§2.1) |
| CK-212 | YES | §6 Increment 1: "manifest for gpu-force only, hand-checked… spring_len capsule + backend absence + why-capsule. Kills the two measured lies. No other UI change." |
| CK-213 | YES | Entire document develops per-engine capability manifests; §8 keeps C1/C3 as merges, not substitutions |
| CK-214 | YES | §2: "Controls default to dimensionless multipliers around a resolved base (adopting B1's write-through semantics…)" |
| CK-215 | NO | Remote engine without manifest → defined "honest fallback form" (§2.1); new graph classes need no new artifact (predicates re-evaluate); manifests bounded by engine count |
| CK-216 | NO | Resolution always labeled ("auto: 25/25 bonds UFF-typed"), why-capsule expandable, override explicit |
| CK-217 | NO | why-capsule: "presets fast/balanced/pretty hidden (vault-tuned)" (§1) |
| CK-218 | NO | Advanced is manifest-filtered; the only escape (override on a collapsed-data row) edits a truthful multiplier and is marked `custom` (§1) |
| CK-219 | NO | Walkthroughs and §3 data flow name concrete state (typed_coverage, n_nodes, has_authored, engine_kind) throughout |

Checklist: 19 total, 18 YES, 1 NO (CK-209, important), 0 essential failures, 0 pitfall triggers.

### Rubric dimensions

**Six-Use-Case Coverage Depth (w 0.16) — score 4**
Axis: "Every case gets a concrete panel-state walkthrough, not a deferral to existing forms." Closer to score_4 ("## Walkthrough: remote engine (case 5)… controls render from the engine's existing /compute/engines settings JSON… falls back to the same generic renderer over {}") — artifact §4.5-6: "Remote engine with manifest. Panel renders from the served manifest exactly as local… broker unreachable → …last cached manifest marked `stale`" and "Remote engine without manifest + static solver. Fallback generic form… `execution: "one_shot"` — the panel renders Solve + quality enum, no live-sim rows". Further from score_2 ("Remote engines and static solvers keep their existing forms for now") — no case is deferred; seventh-case behavior defined for both axes (manifest-less engine → generic fallback; new graph class → predicate re-evaluation). Placement: matches score_4. Improvement: split case 6 into two walkthroughs (remote-without-manifest and static are compressed into one).

**Control Honesty and Applicability (w 0.18) — score 4**
Axis: "Escape surfaces obey the same applicability truth, not reintroduce raw knobs." Closer to score_4 ("The advanced drawer emits only rows whose applicability predicate is live… the spring_len row is absent here too") — artifact §1: "Advanced — the manifest-filtered complete form: every dimension of the engine, each either a live control or a collapsed-data capsule. Dead knobs cannot appear here either (CK-202)". Further from score_2 ("An 'Advanced' disclosure reveals the raw GpuForceOptions sliders") — no raw-knob surface exists anywhere; the single escape (override) expands to a multiplier that truthfully "scales data-owned rests proportionally when partial" (§2 manifest note) and is marked custom. Unit collision resolved structurally: multipliers around resolved bases + "never absolutes for multiplier dims" persistence (§3). Placement: matches score_4. Improvement: state explicitly whether an override at 100% data coverage is physically meaningful or should be refused.

**Provenance Legibility (w 0.12) — score 4**
Axis: "Absence is explained with per-parameter provenance, not left silent or re-aggregated." Closer to score_4 ("a why-capsule reads '25 bond lengths from UFF import — spring length is data-owned'; the old 'sliders scale on top' banner is deleted, not reworded") — artifact §1: "▸ 25/25 rests from UFF (override)" plus expandable per-dimension table ("rests UFF bond table · 25/25 edges / weights UFF atom weights · 24/24 nodes / yours no overrides") and §4.2: "the false 'sliders scale on top' banner is gone — replaced by per-row truth". Further from score_2 ("not rendered; banner reads 'Molecular parameters active'") — provenance is per-parameter lines, never an aggregate claim; parked overrides surfaced ("2 parked overrides", §5). Placement: matches score_4. Improvement: none material; could show the why-capsule for a partial-coverage graph.

**Operate-Mode UX Quality (w 0.12) — score 4**
Axis: "Glanceable state with few live controls, not the same dense form plus another toggle." Closer to score_4 ("Default view: regime capsule ('vault-large — 12,400 nodes') plus an intent row…; raw rows live in a collapsed drawer") — artifact §1 default: engine row, "regime Molecular · UFF · why ▸" capsule, one collapsed data row, three multiplier rows, one toggle, settle/halt line, "Advanced ▸ (manifest-filtered form)" collapsed. Further from score_2 ("'Molecular mode' checkbox above the existing ten sliders and three preset buttons") — the ten-slider form is gone, not toggled. Placement: matches score_4; slightly denser default (Settle + halt + wake/pause visible) keeps it from exceeding. Improvement: tuck the halt/settle line behind the run-state band for the molecular default.

**Constraint Feasibility and Data-Flow Specificity (w 0.12) — score 4**
Axis: "Named existing signals drive rendering, not unspecified 'state'." Closer to score_4 ("Row emission keys off TYPED_FORCE_SUMMARY (typed_nodes, typed_edges) plus node count; remote controls render from the engine's existing /compute/engines settings JSON") — artifact §3: "capability state (memo, keyed on snapshot rev): typed_coverage = TYPED_FORCE_SUMMARY… n_nodes, has_authored, engine_kind" → render → "write-through into engine options" → PanelState. Further from score_2 ("reads the current graph state and renders the appropriate controls") — every stage named. schemars presence grep-verified; `#[capability]` attribute (a new proc-macro crate) is phase-3-quarantined. Placement: matches score_4. Improvement: offer a non-proc-macro encoding (doc-comment convention) for the phase-3 attributes to respect "no new crates" strictly.

**Migration and Persistence Soundness (w 0.12) — score 3**
Axis: "Old payloads migrate with a defined fallback, not discarded on a version bump." Closer to score_4 than score_2 but short of it. Past score_2 ("bump the key… old settings are discarded") — artifact §3: "legacy absolutes are reinterpreted once as multipliers against the *vault* base (their historic meaning) and stored under the vault regime"; §5: "molecule users keep `?config` boot path untouched". Short of score_4 ("missing regime field → resolve…; unparseable → for_n_nodes fallback. Writer emits v2 with schema: 2") — artifact lacks: any version field on the persisted record (the new key is never named) and any unparseable/missing fallback; `seed_strategy`/`seed_custom` fields unmentioned. Placement: past score_2, short of score_4, no clear lean → 3. Improvement: specify the v2 key, `schema: 2` writer, and the unparseable → `for_n_nodes` fallback in one paragraph.

**Kaizen Phasing (w 0.08) — score 4**
Axis: "Phase 1 ships alone and deletes a named lie, not bundles the whole design." Closer to score_4 ("Phase 1 (ships alone): truthful banner + applicability-filtered rows for the local engine — the lying spring_len slider is deleted") — artifact §6 Increment 1: "manifest for gpu-force only, hand-checked against GpuForceOptions; panel renders spring_len capsule + backend absence + why-capsule. Kills the two measured lies. No other UI change." Further from score_2 ("Phase 1: implement the manifest schema, the renderer, the migration, and the new presets") — migration is Increment 2, generation Increment 3, remote Increment 4, each justified. Placement: matches score_4. Improvement: none.

**Mergeability and Synthesis (w 0.10) — score 4**
Axis: "Shared vocabulary enabling the C1+C2+B1 merge, not a siloed design." Closer to score_4 ("The regime capsule and the capability manifest are one applicability record… B1 intent sliders are adopted… as dimensionless multipliers writing through into GpuForceOptions") — artifact §8: "C1's `regimes/*.yaml` becomes the source of the `regimes` array + the why-capsule content… C3's Molecular/Vault/Remote/Static forms are simply the renderings this manifest produces"; §2 adopts B1 multipliers. Further from score_2 ("never mentions the other proposals… or intent sliders") — all three siblings mapped with shared concepts (why-capsule, applicability predicate, regime). Placement: matches score_4. Improvement: none.

**Weighted sum:** 4×0.16 + 4×0.18 + 4×0.12 + 4×0.12 + 4×0.12 + 3×0.12 + 4×0.08 + 4×0.10 = 0.64+0.72+0.48+0.48+0.48+0.36+0.32+0.40 = **3.88**
Gate: no essential NO → uncapped. Pitfalls: 0 YES → no penalty. **Final: 3.88**

---

## Document 2: solution.b.md — C3 Per-Graph-Class Forms

### Checklist

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walks all six with form wireframes §2.1-2.4 behind them: "1. Vault 10.2k → Vault form… 2. Molecular caffeine → Molecular form… 5. Remote engine → Remote form… 6. Static solver → Static form" |
| CK-202 | YES | §2.2: "No global spring_len knob exists anywhere in this form"; Molecular Advanced is a "read-mostly table of resolved UFF parameters… with per-row override toggles" — no raw knobs |
| CK-203 | YES | Forms are "~40-line compositions" of a shared rsx component kit (§3.1); no JS/new styling anywhere |
| CK-204 | YES | No engine changes anywhere in the document; detection is UI-side predicates over GraphSnapshot |
| CK-205 | **NO** | **No data-flow section exists.** §1 gives detection predicates, §2 forms, §3.1 components, §3.3 persistence — but no named signals/state driving rendering, no edit→engine write path, no re-render trigger. Contract section absent (A §3 and C §3 both have it; B does not) |
| CK-206 | YES | Both branch conditions: "Form count is hard-capped at 5… three distinct sources" gate + "§3.2 Hybrid-class behavior" (≥0.5 Molecular-with-hybrid, <0.5 Vault + `uff partial` chip) |
| CK-207 | YES | TYPED_FORCE_SUMMARY/for_n_nodes/PanelState/engine-kind all grep-verified; "declared bounds" (§2.3) is a small embellishment beyond the served payload but the core claim matches ground truth |
| CK-208 | YES | All four form wireframes are narrow single-column `┌ Layout · <Class> ─┐` panels |
| CK-209 | NO | §3.3: "`jc_layout_v1` migrates into `.vault` verbatim" — no unparseable fallback, no versioned reader/writer, seed_strategy/seed_custom unaddressed, and `?config=` boot presets never mentioned (caffeine-uff.yaml workflow ignored) |
| CK-210 | NO | §3.3 specifies what/where (per-class keys `jc_layout_v2.vault`, `.molecular`, …) but never states precedence among data-owned (L0), preset/regime (L1), and user-set (L2) values when they collide |
| CK-211 | NO | Behavior is driven by four hand-written compiled forms + compiled first-match predicates (§1 table, §2) — new compiled per-class surface, not configuration/data; the cap at 5 bounds but does not change the mechanism |
| CK-212 | YES | §5 Increment 1: "Molecular form only (the measured-defect class) + class detection with manual override… Kills all three lies with one new ~150-line form" |
| CK-213 | YES | Entire document develops the class-switch + bounded forms; merge notes (§7) don't substitute the pattern |
| CK-214 | YES | Adopted concretely where applicable: "Geometry scale — … (B1's Spread, specialized)" (§2.2); vault keeps absolutes — an explicit partial-adoption stance |
| CK-215 | NO | "Form count is hard-capped at 5… a new graph class uses the Vault form… until three distinct sources of that class exist" (§3.1) — bounded with defined seventh-case behavior |
| CK-216 | NO | Detection shown as title chip "Molecular ▾" with manual override menu incl. `auto` (§1); reasons visible |
| CK-217 | NO | Molecular form has no preset row; why ▸: "vault presets hidden (would clobber UFF rests)" (§2.2) |
| CK-218 | NO | Molecular Advanced = read-mostly resolved-UFF table (§2.2); Remote form renders the engine's own settings under an honesty header, not raw local GpuForceOptions |
| CK-219 | NO | Forms, chips, thresholds (≥0.5), and quality→iterations mapping (§2.4) are all concrete |

Checklist: 19 total, 15 YES, 4 NO (CK-205 essential; CK-209/210/211 important), **1 essential failure**, 0 pitfall triggers.

### Rubric dimensions

**Six-Use-Case Coverage Depth (w 0.16) — score 3**
Axis: concrete walkthroughs, not deferrals. Past score_2 ("Generated and importer graphs are untyped, so they behave like vault") — but uncomfortably close to it for two cases: §4.3 "Generated grid 12×12 → Vault form + `generated` chip in the title; n=144 → backend row hidden" and §4.4 "Untyped importer graph → Vault form + `untyped edges` chip" are deferrals-to-Vault-form with deltas, rescued by §2.1's full Vault wireframe and the specific chip/backend details. Cases 1/2/5/6 have full purpose-built wireframes (§2.1-2.4); hybrid behavior (§3.2) and seventh-case gate (§3.1) are defined. Short of score_4's per-case concrete panel-state standard on cases 3-4. Placement: past score_2, short of score_4, no clear lean → 3. Improvement: give generated and importer graphs their own state walkthroughs (what the capsule says, what n-tuned defaults resolve to), not "Vault form + chip".

**Control Honesty and Applicability (w 0.18) — score 4**
Axis: escape surfaces obey applicability truth. Closer to score_4 ("advanced drawer emits only live rows… spring_len absent here too") — §2.2: "No global spring_len knob exists anywhere in this form — at full coverage it is meaningless, at partial coverage Geometry scale governs the remainder and says so (`scales UFF rests + 13 untyped edges`)"; Advanced is a resolved-UFF table with per-row overrides. Further from score_2 — no raw-knob drawer exists; the Vault form keeps the flat sliders but for vaults every one is live ("Every knob is meaningful here", §2.1); the <50% hybrid case routes to Vault where spring_len genuinely governs the untyped majority (§3.2). Unit collision resolved for the molecular class via ×1.0 multipliers. Placement: matches score_4. Improvement: state what honesty guarantee applies to a 49%-typed graph's spring_len label in the Vault form (it governs 13 of 25 edges — say so on the row).

**Provenance Legibility (w 0.12) — score 4**
Axis: per-parameter provenance, banner corrected. Closer to score_4 — §2.1: "the `n-tuned` provenance tag on spring_len; the misleading banner absent (it was false only for typed graphs…)"; §2.2 why ▸ expansion: "element list (C, H, N, O), bond-order histogram, authored-coordinate source (`sdf.toml seed = "authored"`), preset line `vault presets hidden (would clobber UFF rests)`"; hybrid sub-line states exactly what the multiplier governs. Further from score_2 — absence is never silent; the banner is explicitly removed, not reworded. Placement: matches score_4. Improvement: carry a per-row provenance tag into the Remote form (currently the header is the only provenance).

**Operate-Mode UX Quality (w 0.12) — score 3**
Axis: glanceable few-control default, not the dense form plus a toggle. The Molecular form matches score_4 (engine row, UFF summary, two multipliers, one toggle, settled line, collapsed Advanced). But the Vault form — the default and most common case — is "Essentially today's panel" (§2.1): presets row + six sliders + cooling row + backend/seed row + halt line. That is the score_2 anchor's "existing ten sliders and three preset buttons" minus the checkbox (the class chip is better than a checkbox, but the density stands). Past score_2 (molecular default is genuinely calm; chips carry provenance; no bolt-on toggle), short of score_4 (the default-case form remains the dense form; no intent row for vault). Placement: 3. Improvement: apply the intent-multiplier row (already designed for Molecular) to the Vault form and collapse the absolute sliders into its Advanced drawer.

**Constraint Feasibility and Data-Flow Specificity (w 0.12) — score 3**
Axis: named signals drive rendering. Past score_2 ("The panel reads the current graph state and renders the appropriate controls") — detection does name `TYPED_FORCE_SUMMARY`, GraphSnapshot source kind, and engine kind (§1 table), and `for_n_nodes` is named in §4.4; engine kind strings verified against the proto. Short of score_4 — artifact lacks: any rendering pipeline (what signal/memo drives re-render on snapshot swap), any edit→engine write path, any persistence-record shape beyond key names. The §2.3 "within declared bounds" claim assumes a schema the endpoint does not serve today. Placement: 3. Improvement: add the missing data-flow section (this is also the CK-205 essential failure).

**Migration and Persistence Soundness (w 0.12) — score 3**
Axis: defined migration + fallback, not discard. Past score_2 — "jc_layout_v1 migrates into `.vault` verbatim (its historic meaning)" is a sensible explicit target, and per-class keys concretely fix the real vault→molecule clobber (§3.3). Short of score_4 — artifact lacks: unparseable/missing fallback, versioned reader/writer, seed-field handling, and any `?config=` boot-preset compatibility story (the caffeine-uff.yaml workflow is never mentioned). Placement: 3 (the weakest of the three on this axis). Improvement: one paragraph on `?config=` interaction and the unparseable fallback.

**Kaizen Phasing (w 0.08) — score 4**
Axis: phase 1 ships alone and deletes a named lie. Closer to score_4 — §5 Increment 1: "Molecular form only (the measured-defect class) + class detection with manual override; other classes keep today's panel as 'Vault form'. Kills all three lies with one new ~150-line form" — the three lies named explicitly in §4.2 ("dead spring_len, false banner, vault presets"). Further from score_2 — persistence, remote, static are increments 2-4, each small and justified; no engine work anywhere. Placement: matches score_4. Improvement: none.

**Mergeability and Synthesis (w 0.10) — score 4**
Axis: shared vocabulary for the merge. Closer to score_4 — §7: "C2's manifest is the mechanism that would *generate* these forms from data… C3 is the design target…, C2 the engine"; "class detection predicates and C1's regime-resolution predicates are the same ordered-ruleset; the Molecular form's `why ▸` is C1's regime capsule"; "B1's intent multipliers appear as Geometry scale / Repulsion (atoms)". Further from score_2 — all siblings and B1 addressed with concrete adopt-where positions. Placement: matches score_4. Improvement: none.

**Weighted sum:** 3×0.16 + 4×0.18 + 4×0.12 + 3×0.12 + 3×0.12 + 3×0.12 + 4×0.08 + 4×0.10 = 0.48+0.72+0.48+0.36+0.36+0.36+0.32+0.40 = **3.48**
Gate: **CK-205 (essential) = NO → capped at 2.0**, overall_label cannot exceed hold. Pitfalls: 0 YES → no penalty. **Final: 2.00 (gate-capped)**

---

## Document 3: solution.c.md — C1 Regime-as-Data Registry

### Checklist

| id | answer | evidence |
|---|---|---|
| CK-201 | YES | §4 walks all six ("1. Vault 10.2k… 2. Molecular caffeine… 3. Generated grid… 4. Untyped importer… 5. Remote engine… 6. Static solver"), each with resolved regime, panel state, and provenance line; remote + static have worked YAML in §1.1 |
| CK-202 | YES | §2: "Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere (CK-202)"; `spring_len: null` → capsule, not knob |
| CK-203 | YES | Pure YAML + serde + Dioxus bands; "serde(deny_unknown_fields)" (§1.1); no JS, no styling pipeline |
| CK-204 | YES | Core = registry loader + resolver + panel; engine structs only read ("options must deserialize against the named engine's options struct", §1.1); LayoutPreset deletion is UI-side (layout.rs:1079 — verified) |
| CK-205 | YES | §1 registry/schema, §2 panel IA + ASCII wireframe, §3 data flow (named pipeline), §4 walkthroughs, §5 persistence/migration, §6 kaizen, §7 judge-condition responses |
| CK-206 | YES | Branch condition (its pruning gate): "Pruning gate (remote/static never named): both are first-class registry citizens with worked YAML (§1.1) and walkthroughs (§4.5–6)" — ray-force-3d.yaml and force-atlas-static.yaml are fully worked |
| CK-207 | YES | TYPED_FORCE_SUMMARY/for_n_nodes/jc_layout_v1/caffeine-uff.yaml/LayoutPreset+detect all grep-verified consistent; `settings_schema: broker` assumes a schema-serving endpoint that doesn't exist yet (phase 4) but matches the ground-truth text |
| CK-208 | YES | §2 wireframe is a narrow three-band single column inside `┌ Layout ─┐` |
| CK-209 | NO | §5: "Legacy `jc_layout_v1` absolutes migrate into `custom` regime overrides against `vault-large`" — explicit rule, but no unparseable/missing fallback, no version field on the v2 record, seed fields unmentioned; old-format user boot configs break silently under "one loader, one schema" (only the shipped caffeine-uff.yaml is migrated) |
| CK-210 | YES | §3: "effective options = regime.options ∘ user_overrides"; "null + data-owned dims filled"; "Overrides are stored by control id against the regime base, so a regime YAML edit… propagates" — L0 > L2 > L1 stated |
| CK-211 | YES | The purest packages-not-crates answer: "presets become deployable data" (§3); presets migrated to YAML, Rust enum deleted (§5); behavior = registry data |
| CK-212 | YES | §6 Increment 1: "registry schema + loader + `molecular-uff.yaml` + resolver covering only the typed-coverage predicate; panel gains regime capsule + collapsed-data rows. Today's sliders otherwise unchanged. Kills the three measured lies" |
| CK-213 | YES | Entire document develops regime-as-data; §8 positions it as the data layer of the shared pattern without substituting |
| CK-214 | YES | "B1's intent multipliers are the `kind: multiplier` controls" (§8); geometry_scale/repulsion multipliers worked in molecular-uff.yaml |
| CK-215 | NO | Catch-alls are defined: ray-force-3d.yaml's `applicability: {engine_kind: remote}` + `settings_schema: broker` covers ANY remote engine generically; `engine_kind: static` covers one-shots; untyped/unknown graphs fall to vault regimes by predicate (§4.4). New specialization = a YAML file (data), not code |
| CK-216 | NO | "The resolution result is always shown with its reason: `Molecular · UFF (auto: 25/25 bonds UFF-typed)`" (§1.2); manual picker override with `auto` restore |
| CK-217 | NO | `presets_hidden: [fast, balanced, pretty]   # vault-tuned; quarantined for this regime` (§1.1) |
| CK-218 | NO | "Advanced shows the regime's full option set, data-owned entries as capsules" (§2) — no raw GpuForceOptions dump on any surface |
| CK-219 | NO | Resolver algorithm, five-field state record, YAML schema, and walkthroughs are concrete; no "adapts appropriately" language |

Checklist: 19 total, 18 YES, 1 NO (CK-209, important), 0 essential failures, 0 pitfall triggers.

### Rubric dimensions

**Six-Use-Case Coverage Depth (w 0.16) — score 4**
Axis: concrete walkthroughs, not deferrals. Closer to score_4 — remote and static (its own pruning-gate failure) are the strongest coverage in the set: worked `ray-force-3d.yaml` ("settings_schema: broker — the registry entry declares NO constants of its own") and `force-atlas-static.yaml` ("execution: one_shot → Solve + quality enum (mapping declared in YAML) + `last solved 12:03 · 1.2s`"), with broker-down behavior ("Status backoff line; cached schema marked `stale`"). Further from score_2 — no case deferred; seventh-case behavior defined via catch-all predicates (any remote engine matches `engine_kind: remote`; untyped graphs fall to vault by predicate). Placement: matches score_4. Improvement: walk a *new* remote engine with no bespoke entry through the catch-all explicitly as a seventh-case demo.

**Control Honesty and Applicability (w 0.18) — score 4**
Axis: escape surfaces obey applicability truth. Closer to score_4 — "`spring_len: null  # null = data-owned (UFF rests); panel shows capsule`" and "Advanced shows the regime's full option set, data-owned entries as capsules — dead knobs unrepresentable everywhere (CK-202)"; `presets_hidden` quarantines wrong-regime presets structurally. Further from score_2 — no raw-knob surface; unit collision resolved via `kind: multiplier` controls with `applies_to: [spring_len, typed_rests]` against the regime base. Vault regimes keep absolute sliders — honest for vaults (that IS their regime). Placement: matches score_4. Improvement: in the molecular Advanced, mark vault-regime knobs (gravity) with the caution treatment rather than presenting them as plain live rows.

**Provenance Legibility (w 0.12) — score 3**
Axis: absence explained per-parameter, banner corrected. Past score_2 — provenance is real: "The resolution result is always shown with its reason: `Molecular · UFF (auto: 25/25 bonds UFF-typed)`" (§1.2) and data-owned dims render as "▸ 25/25 rests from UFF (override)" capsules (§2) — nearly the score_4 capsule text. Short of score_4 — the anchor's second clause is "the old 'sliders scale on top' banner is deleted, not reworded": **the word "banner" never appears in solution.c.md**; §6 Increment 1 says "Kills the three measured lies" without naming them (unlike B, which names the false banner explicitly). Ambiguity about the banner's fate → lower placement per strictness. Placement: past score_2, short of score_4, leans toward 4 but the unnamed banner holds it at 3. Improvement: one line in §2 or §7 explicitly deleting the layout.rs:3606-3609 banner.

**Operate-Mode UX Quality (w 0.12) — score 4**
Axis: glanceable few-control default. Closer to score_4 — §2 default: engine picker, regime picker with resolution reason ("auto: 25/25 bonds UFF-typed"), one collapsed data capsule, exactly the regime's three controls (Geometry scale, Repulsion, Keep authored), run-state line, "Advanced ▸". Band 2 is "Exactly the regime's `controls` list, no more" — few meaningful controls, one primary action, no slop elements. Further from score_2 — the dense form survives only as the vault regime's honest declaration, not as the universal default. Placement: matches score_4. Improvement: none material.

**Constraint Feasibility and Data-Flow Specificity (w 0.12) — score 4**
Axis: named signals drive rendering. Closer to score_4 — §3: "registry load (boot): parse + validate all regimes/*.yaml → GraphSnapshot swap ──► snapshot state record (5 fields: typed_bond_coverage, n_nodes, has_authored, source_kind, engine_kind) → resolver → effective options = regime.options ∘ user_overrides → panel renders regime.controls → persist: jc_layout_v2". Every stage named; all sources in the ground-truth list; no JS/crates/engine changes in core. Further from score_2 — nothing hand-waved. Two small dings (not score-changing): "data-owned dims filled from TYPED_FORCE_SUMMARY" mischaracterizes the signal (it holds counts; values flow via TypedForceParams), and `settings_schema: broker` assumes schema-serving /compute/engines (verified absent today; phase-4 item, unacknowledged as broker work). Placement: matches score_4. Improvement: acknowledge the broker schema endpoint as a phase-4 server-side dependency with a values-only fallback (A's fallback form).

**Migration and Persistence Soundness (w 0.12) — score 3**
Axis: defined migration + fallback. Past score_2 — explicit rules: "Legacy `jc_layout_v1` absolutes migrate into `custom` regime overrides against `vault-large` (their historic base)"; presets migrate to YAML with `applicability: {engine_kind: local}`, "the Rust enum and its detect-fingerprinting heuristic are deleted (clean cutover)" (verified accurate); `caffeine-uff.yaml` moves into the registry with one loader/schema. Short of score_4 — artifact lacks: unparseable/missing fallback, version field on jc_layout_v2, seed-field handling; and "one loader, one schema" silently breaks user-authored old-format boot configs (only the shipped file is migrated). Placement: 3. Improvement: keep a legacy-shape reader for `?config=` payloads and state the unparseable → `for_n_nodes` fallback.

**Kaizen Phasing (w 0.08) — score 3**
Axis: phase 1 ships alone, deletes a named lie, doesn't bundle. Past score_2 — increments 2-4 (preset migration + enum deletion; multiplier controls; remote/static entries) are properly deferred and justified, so no big-bang. Short of score_4 — the anchor's phase 1 is "truthful banner + applicability-filtered rows" *without* infrastructure; C's Increment 1 bundles "registry schema + loader + `molecular-uff.yaml` + resolver" to achieve the same lie-kill that A's phase 1 achieves with one hand-checked manifest — heavier than the smallest shippable increment, and "the three measured lies" are referenced but never enumerated in the document. Placement: 3. Improvement: split Increment 1 into (a) capsule + collapsed-data rows against a single hardcoded molecular-uff entry, (b) the loader/resolver generalization.

**Mergeability and Synthesis (w 0.10) — score 4**
Axis: shared vocabulary for the merge. Closer to score_4 — §8: "C2's capability manifests slot in as the per-engine `controls`/applicability truth (a manifest is the engine-side half of a regime entry); B's four forms are what this registry renders…; B1's intent multipliers are the `kind: multiplier` controls"; §1.2: "this is the same predicate record as Solution B's class detection; they share one implementation". Further from score_2 — one-pattern observation embraced, both siblings mapped, B1 position concrete. Placement: matches score_4. Improvement: none.

**Weighted sum:** 4×0.16 + 4×0.18 + 3×0.12 + 4×0.12 + 4×0.12 + 3×0.12 + 3×0.08 + 4×0.10 = 0.64+0.72+0.36+0.48+0.48+0.36+0.24+0.40 = **3.68**
Gate: no essential NO → uncapped. Pitfalls: 0 YES → no penalty. **Final: 3.68**

---

## Summary

### Final scores and labels

| Rank | Document | Raw | Adjusted | overall_label |
|---|---|---|---|---|
| 1 | solution.a.md (C2 capability manifests) | 3.88 | **3.88** | **select** |
| 2 | solution.c.md (C1 regime-as-data) | 3.68 | **3.68** | **hold** (merge contributor) |
| 3 | solution.b.md (C3 per-class forms) | 3.48 | **2.00** (essential-gate capped) | **reject** (gate-capped) |

### rank_justification

- **A over C (3.88 vs 3.68):** A matches the score_4 anchor on all three dimensions where C falls to 3. C never mentions the false banner (Provenance 3 vs A's explicit "the false 'sliders scale on top' banner is gone"), and C's phase 1 bundles schema+loader+resolver where A's phase 1 is one hand-checked manifest killing the same lies (Kaizen 3 vs 4). Migration is a 3 for both. On the tie-break dimensions (Control Honesty, Coverage) they are level at 4/4 — the decision rests on provenance explicitness and phase-1 minimality, both favoring A. A also has the stronger CK-005 answer (manifest as build-time projection of the serde schema — drift is a compile error), which is exactly the concern the selection judges attached to C2.
- **C over B despite B's higher raw score (3.68 vs capped 2.00):** B is the only document missing a contract-mandated section — no data flow (CK-205 essential NO). Per the specification's essential_gate, that caps B at 2.0 and forbids a label above hold; per the label rules, gate-capped ⇒ reject. This is mechanical, not discretionary: the missing section leaves B's rendering pipeline, edit write-path, and re-render triggers unspecified — ungradeable on Constraint Feasibility at the score_4 level and unshippable as docs/layout-ux.md material. B's underlying design is otherwise strong (Control Honesty 4, Kaizen 4, the best phase-1 story of the three), which is why its raw 3.48 still exceeds a mediocre document — but the gate governs.
- Tie-breaks not needed (no adjusted-score ties).

### merge_map

Primary pattern: **solution.a.md (C2 capability manifests)**. Merge in:

**From solution.c.md (C1):**
1. **The YAML regime registry + ordered first-match resolver** as the data layer backing A's manifest `regimes` array and why-capsule content — C §8 already frames this ("a manifest is the engine-side half of a regime entry"); A §8 already names it ("C1's regimes/*.yaml becomes the source of the regimes array + the why-capsule content").
2. **`presets_hidden` quarantine mechanism** (C §1.1) — structural, data-driven answer to CK-217; cleaner than A's capsule-note-only treatment.
3. **Schema convergence with `?config=` boot presets** (C §1.1/§5: one loader, one schema, `deny_unknown_fields`) — but with a legacy-shape reader retained (C's silent old-format break must be fixed in the merge).
4. **Preset migration to YAML** — fast/balanced/pretty as registry entries with `applicability: {engine_kind: local}`; delete `LayoutPreset` + `detect` (verified at layout.rs:1079/3584). This is the packages-not-crates endgame A defers.
5. **Override-against-regime persistence** (C §3: overrides keyed by control id against the regime base so YAML edits propagate) — strictly better than A's multiplier-keyed overrides alone; adopt inside A's `{regime_id, overrides}` record.
6. **Catch-all regime predicates** (`engine_kind: remote` + `settings_schema: broker`, `engine_kind: static`) as A's seventh-case story — one entry covers any new remote engine/static solver.

**From solution.b.md (C3):**
1. **Geometry scale's hybrid sub-line** ("scales UFF rests + 13 untyped edges", B §3.2) — adopt as the manifest `note` pattern for partial-coverage multiplier dimensions; more truthful than A's generic note.
2. **The explicit three-lies naming** (B §4.2: "dead spring_len, false banner, vault presets") — use in phase-1 framing and the banner-deletion line A already has.
3. **The Static form's exact shape** (Solve + quality enum with declared `maps_to` iteration mapping + last-run line, B §2.4) — A's `execution: "one_shot"` rendering should copy B's quality-mapping concreteness.
4. **Per-class persistence separation insight** (B §3.3: switching class must never clobber the other class's tuning) — fold into A's parked-override lifecycle: parked overrides should be keyed per regime, not globally.
5. **The form-cap / three-sources discipline** (B §3.1) — translate to manifest-authorship: new applicability predicates require the same three-sources bar, keeping the closed predicate set closed.
6. B1 intent sliders are already adopted by the primary (A §2, increment 2); B's Molecular-form specialization (Geometry scale) confirms the control set.

**Nothing from B's architecture merges** — the compiled per-class forms and class-switch are superseded by the primary's manifest rendering (as B itself concedes in §7: "C2 the engine for getting there").

### Ground-truth discrepancies to flag upstream

1. The ground truth (and all three docs) treat `/compute/engines` as serving per-engine settings JSON; the verified payload is `{id, display_name, description, kind}` only (`compute_broker.rs:609-614`). Settings transport exists (Subscribe `params` as serde_json), but any design phase relying on a served *schema* (C's `settings_schema: broker`, A's phase-4 manifest serving, B's "declared bounds") requires broker-side work that none of the three documents prices in. The merged design must adopt A's values-only generic fallback as the default remote story.
2. `GpuForceOptions::for_n_nodes` lives in `crates/graph-layouts/src/layout/algorithms/gpu_force.rs`, not `crates/graph-api/src/compute.rs` — substance verified, path in some notes is stale.

---

## Self-verification

| # | Question | Answer | Adjustment |
|---|---|---|---|
| 1 | Did I examine all relevant files? | Yes — spec in full (527 lines), all three solutions in full, both ground-truth docs, and grep-verified every codebase-anchored claim (banner, TYPED_FORCE_SUMMARY, PanelState/jc_layout_v1, for_n_nodes, LayoutPreset+detect, caffeine-uff.yaml, schemars, /compute/engines EngineView, engine kind strings). | None |
| 2 | Am I influenced by length/tone? | All three docs are near-identical length (147/149/160 lines); scores derive from quoted anchor comparisons, not prose confidence. B's confident tone did not prevent the CK-205 NO. | None |
| 3 | Anchor fidelity — both sides quoted before every score, on-axis? | Yes — each dimension above names closer_to and further_from anchors with quoted artifact evidence before the number, on the dimension's contrast axis. B's Operate-Mode 3 was checked specifically against the score_2 "checkbox + dense form" anchor rather than a general density impression. | None |
| 4 | Is my reference result itself correct? | Cross-checked against the spec's contract sections and the context doc's measured audit; the reference's "banner must be explicitly deleted" requirement comes from the Provenance dimension instruction itself, so applying it to C is spec-mandated, not invented. | None |
| 5 | Are scores proportional, not uniformly harsh/lenient? | Scores span 2.00-3.88 adjusted; 3s and 4s both used with quoted justification; no 5s were awarded because no quoted evidence clearly exceeded a score_4 anchor on its axis; no 1-2s because no document matched or fell below a score_2 anchor on any axis. The single gate application (B) follows the spec mechanically. | None |

## Strengths

1. solution.a: phase-1 minimality + CK-005 answer (manifest as build-time projection, drift = compile error) — §2.1/§6.
2. solution.a: multiplier-keyed persistence ("never absolutes… vault values can't leak onto ångström geometry", §3) — structural fix for the unit collision.
3. solution.c: remote/static as first-class worked YAML with catch-all predicates — the strongest seventh-case story (§1.1).
4. solution.c: override-against-regime persistence making presets deployable data (§3).
5. solution.b: best phase-1 (one ~150-line form kills three named lies, §5) and the hybrid sub-line pattern (§3.2).

## Issues

1. **High** — solution.b: missing data-flow section (CK-205 essential) → gate cap 2.0, reject. Evidence: §1-§7 contain no named-signal rendering pipeline. Suggestion: add the pipeline (detection memo → form selection → edit write-through → persist) and re-evaluate; raw score 3.48 indicates a viable merge contributor once cured.
2. **Medium** — solution.c: false banner never mentioned; "three measured lies" never enumerated (Provenance 3). Suggestion: one explicit deletion line citing layout.rs:3606-3609.
3. **Medium** — all three: no unparseable/missing fallback and no version field in migration (CK-209 NO ×3); seed_strategy/seed_custom dropped silently from every new persistence schema. Suggestion: adopt the score_4 anchor's reader/writer rules in the merged design.
4. **Medium** — all three: remote settings-schema reliance exceeds the verified /compute/engines payload. Suggestion: default to A's values-only generic fallback; treat schema-serving as priced broker work.
5. **Low** — solution.b: ?config= boot presets never mentioned; caffeine-uff.yaml workflow unaddressed.
6. **Low** — solution.a: `#[capability]` proc-macro attribute (phase 3) would add a new crate; offer a non-macro encoding.

## Confidence

- Level: **High**
- evidence_strength: Strong (every codebase-anchored claim grep-verified with file:line)
- criterion_clarity: Clear (anchors and gates unambiguous; the one judgment call — B's CK-205 — is a binary section-presence check)
- specification_quality: Complete (all dimensions carry full anchors; gates, aggregation, ranking, and labels fully specified)
