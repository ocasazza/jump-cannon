# Must FAIL to render: terminusdb is enabled but no admin password Secret is
# named — the admin password must come from a user-created Secret, never
# from values.
terminusdb:
  enabled: true

tests:
  fuzz:
    enabled: false
  performance:
    enabled: false
  browser:
    enabled: false
